/* sh3-css-inline: injected styles */
(function(){
  const s=document.createElement("style");
  s.textContent=".r2-form.svelte-h3b7wu{display:flex;flex-direction:column;gap:8px;max-width:480px;padding:12px}.r2-form.svelte-h3b7wu label:where(.svelte-h3b7wu){display:flex;flex-direction:column;gap:2px;font-size:.9em}.r2-form__error.svelte-h3b7wu{color:#e66;margin:0}.r2-form__hint.svelte-h3b7wu{align-self:flex-start;background:transparent;border:none;color:var(--sh3-accent, #4a9eff);padding:0;font:inherit;cursor:pointer;text-decoration:underline;text-underline-offset:2px}.r2-form__actions.svelte-h3b7wu{display:flex;gap:8px;justify-content:flex-end;margin-top:8px}.r2-guide.svelte-122r6hr{background:var(--sh3-surface, #1a1a1a);border:1px solid var(--sh3-border, #2a2a2a);border-radius:4px;padding:12px 16px;overflow:auto;max-height:100%}.r2-guide__header.svelte-122r6hr{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px}.r2-guide__header.svelte-122r6hr h3:where(.svelte-122r6hr){margin:0;font-size:1em}.r2-guide__close.svelte-122r6hr{background:transparent;color:inherit;border:none;font-size:1.4em;line-height:1;cursor:pointer;padding:0 4px}.r2-guide__close.svelte-122r6hr:hover{color:var(--sh3-accent, #4a9eff)}.r2-guide__steps.svelte-122r6hr{padding-left:1.25em;display:flex;flex-direction:column;gap:10px;font-size:.9em}.r2-guide__steps.svelte-122r6hr li:where(.svelte-122r6hr){line-height:1.45}.r2-guide__steps.svelte-122r6hr code:where(.svelte-122r6hr){font-size:.9em;background:var(--sh3-bg, #111);padding:1px 4px;border-radius:2px}.r2-guide__steps.svelte-122r6hr a:where(.svelte-122r6hr){color:var(--sh3-accent, #4a9eff)}.r2-guide__steps.svelte-122r6hr pre:where(.svelte-122r6hr){font-size:.82em;background:var(--sh3-bg, #111);padding:8px;border-radius:3px;overflow:auto;margin:6px 0 0}.r2-targets.svelte-16gewpp{padding:12px}.r2-targets--split.svelte-16gewpp{display:grid;grid-template-columns:1fr 1fr;gap:16px;align-items:start}@media(max-width:720px){.r2-targets--split.svelte-16gewpp{grid-template-columns:1fr}}header.svelte-16gewpp{display:flex;justify-content:space-between;align-items:center}.r2-targets__actions.svelte-16gewpp{display:flex;gap:6px}table.svelte-16gewpp{width:100%;border-collapse:collapse;margin-top:8px}th.svelte-16gewpp,td.svelte-16gewpp{text-align:left;padding:6px 8px;border-bottom:1px solid var(--sh3-border, #2a2a2a)}.muted.svelte-16gewpp{color:var(--sh3-muted, #888)}.danger.svelte-16gewpp{color:#e66}.r2-progress.svelte-putn2e{padding:8px;border:1px solid var(--sh3-border, #2a2a2a);border-radius:3px;margin:8px 0}.r2-progress__bar.svelte-putn2e{height:6px;background:var(--sh3-surface, #1a1a1a);margin:4px 0}.r2-progress__fill.svelte-putn2e{height:100%;background:var(--sh3-accent, #4a9eff)}.r2-progress__counts.svelte-putn2e{font-size:.85em;color:var(--sh3-muted, #888)}.r2-backup.svelte-8kfsmf{padding:12px;display:flex;flex-direction:column;gap:8px}label.svelte-8kfsmf{display:flex;flex-direction:column;gap:2px;max-width:420px}select.svelte-8kfsmf{padding:var(--shell-pad-md) var(--shell-pad-lg);background:var(--shell-input-bg);color:var(--shell-fg);border:1px solid var(--shell-border);border-radius:var(--shell-radius);font-family:inherit;font-size:.8125rem;line-height:var(--shell-line)}select.svelte-8kfsmf:focus-visible{border-color:var(--shell-input-border-focus);box-shadow:var(--shell-focus-ring);outline:none}select.svelte-8kfsmf:disabled{opacity:.55;cursor:not-allowed}.r2-log.svelte-8kfsmf{list-style:none;padding:0;font-size:.85em}.r2-log.svelte-8kfsmf li:where(.svelte-8kfsmf){display:grid;grid-template-columns:auto 1fr auto auto;gap:8px;padding:2px 0}.r2-log.svelte-8kfsmf .status-failed:where(.svelte-8kfsmf){color:#e66}.r2-log.svelte-8kfsmf .status-skipped-unchanged:where(.svelte-8kfsmf){color:var(--sh3-muted, #888)}.muted.svelte-8kfsmf{color:var(--sh3-muted, #888)}.r2-tree.svelte-8cyv1p{width:100%;border-collapse:collapse;font-size:.9em}th.svelte-8cyv1p,td.svelte-8cyv1p{text-align:left;padding:4px 8px;border-bottom:1px solid var(--sh3-border, #2a2a2a)}tr.exists.svelte-8cyv1p{color:var(--sh3-muted, #888)}.badge.svelte-8cyv1p{margin-left:6px;background:var(--sh3-surface, #1a1a1a);padding:0 6px;border-radius:2px;font-size:.75em}.r2-import.svelte-1kdudbk{padding:12px;display:flex;flex-direction:column;gap:8px}label.svelte-1kdudbk{display:flex;flex-direction:column;gap:2px;max-width:420px}.warn.svelte-1kdudbk{background:#5a3a1a;border:1px solid #a77733;padding:8px;color:#ffd}.summary.svelte-1kdudbk{border:1px solid var(--sh3-border, #2a2a2a);padding:8px;border-radius:3px}.actions.svelte-1kdudbk{display:flex;gap:8px;flex-wrap:wrap}";
  document.head.appendChild(s);
})();
var me = Object.defineProperty;
var be = (a, t, r) => t in a ? me(a, t, { enumerable: !0, configurable: !0, writable: !0, value: r }) : a[t] = r;
var ie = (a, t, r) => be(a, typeof t != "symbol" ? t + "" : t, r);
import { mount as le, unmount as ce } from "svelte";
import * as e from "svelte/internal/client";
import "svelte/internal/disclose-version";
import { shell as de } from "sh3-core";
const ge = "targets/";
function _e() {
  const a = new Uint8Array(8);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return `tgt-${t}`;
}
async function ye(a) {
  const t = await a.list(), r = [];
  for (const i of t) {
    if (!i.path.startsWith(ge) || !i.path.endsWith(".json")) continue;
    const o = await a.read(i.path);
    if (o)
      try {
        r.push(JSON.parse(o));
      } catch {
      }
  }
  return r.sort((i, o) => i.label.localeCompare(o.label));
}
async function we(a, t) {
  await a.write(`${ge}${t.id}.json`, JSON.stringify(t, null, 2));
}
async function xe(a, t) {
  await a.delete(`${ge}${t}.json`);
}
function ke(a) {
  const t = a.documents({ format: "text" });
  let r = e.state(e.proxy([])), i = e.state(!1);
  const o = e.proxy({
    running: !1,
    currentLabel: "",
    total: 0,
    processed: 0,
    uploaded: 0,
    skipped: 0,
    failed: 0,
    errors: []
  });
  async function s() {
    e.set(r, await ye(t), !0), e.set(i, !0);
  }
  return {
    ctx: a,
    docs: t,
    get targets() {
      return e.get(r);
    },
    get targetsLoaded() {
      return e.get(i);
    },
    progress: o,
    refreshTargets: s
  };
}
const Ie = {
  md: "text/markdown",
  markdown: "text/markdown",
  json: "application/json",
  txt: "text/plain",
  guml: "text/plain",
  html: "text/html",
  htm: "text/html",
  css: "text/css",
  csv: "text/csv",
  xml: "application/xml",
  svg: "image/svg+xml",
  yaml: "application/yaml",
  yml: "application/yaml"
};
function Se(a, t, r) {
  if (!t) throw new Error("buildObjectKey: shardId is required");
  if (r.split("/").some((s) => s === ".." || s === "."))
    throw new Error(`buildObjectKey: path traversal not allowed: ${r}`);
  const o = r.startsWith("/") ? r.slice(1) : r;
  return `${a}${t}/${o}`;
}
function Ae(a) {
  const t = a.lastIndexOf(".");
  if (t < 0) return "application/octet-stream";
  const r = a.slice(t + 1).toLowerCase();
  return Ie[r] ?? "application/octet-stream";
}
const pe = "0123456789abcdef";
async function ue(a) {
  const t = typeof a == "string" ? new TextEncoder().encode(a) : a instanceof Uint8Array ? a : new Uint8Array(a), r = await crypto.subtle.digest("SHA-256", t), i = new Uint8Array(r);
  let o = "";
  for (let s = 0; s < i.length; s++)
    o += pe[i[s] >>> 4], o += pe[i[s] & 15];
  return o;
}
async function Z(a, t) {
  const r = a instanceof Uint8Array ? a : new Uint8Array(a), i = await crypto.subtle.importKey(
    "raw",
    r,
    { name: "HMAC", hash: "SHA-256" },
    !1,
    ["sign"]
  ), o = await crypto.subtle.sign("HMAC", i, new TextEncoder().encode(t));
  return new Uint8Array(o);
}
const se = "uploads/";
function Ce(a) {
  return a.slice(0, 7);
}
function Te() {
  const a = new Uint8Array(4);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return t;
}
async function Re(a, t) {
  const r = `${se}${Ce(t.at)}/`, i = `${t.at}-${Te()}.json`;
  await a.write(`${r}${i}`, JSON.stringify(t, null, 2));
}
async function Ee(a, t) {
  const r = await a.list(), i = /* @__PURE__ */ new Set();
  for (const n of r) {
    if (!n.path.startsWith(se)) continue;
    const l = n.path.slice(se.length), d = l.indexOf("/");
    d > 0 && i.add(l.slice(0, d));
  }
  const o = Array.from(i).sort().reverse().slice(0, 2), s = [];
  for (const n of r) {
    if (!o.find((u) => n.path.startsWith(`${se}${u}/`)) || !n.path.endsWith(".json")) continue;
    const d = await a.read(n.path);
    if (d)
      try {
        s.push(JSON.parse(d));
      } catch {
      }
  }
  return s.sort((n, l) => n.at < l.at ? 1 : n.at > l.at ? -1 : 0), s.slice(0, t);
}
function Q() {
  const a = new Uint8Array(6);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return `${Date.now().toString(36)}-${t}`;
}
async function ve(a) {
  const { target: t, client: r, logHandle: i, readForeign: o, shardId: s, path: n } = a, l = Se(t.keyPrefix, s, n), d = (/* @__PURE__ */ new Date()).toISOString();
  let u;
  try {
    u = await o(s, n);
  } catch (p) {
    const S = p instanceof Error ? p.message : String(p);
    return await ee(i, { id: Q(), targetId: t.id, shardId: s, path: n, sha256: "", size: 0, status: "failed", reason: S, at: d }), { status: "failed", reason: S };
  }
  if (u === null) {
    const p = "Document not found at source";
    return await ee(i, { id: Q(), targetId: t.id, shardId: s, path: n, sha256: "", size: 0, status: "failed", reason: p, at: d }), { status: "failed", reason: p };
  }
  const w = new TextEncoder().encode(u).length, m = await ue(u);
  try {
    const p = await r.headObject(l);
    if (p && p.sha256 === m)
      return await ee(i, { id: Q(), targetId: t.id, shardId: s, path: n, sha256: m, size: w, status: "skipped-unchanged", at: d }), { status: "skipped-unchanged", sha256: m, size: w };
  } catch {
  }
  try {
    return await r.putObject({
      key: l,
      body: u,
      contentType: Ae(n),
      sha256: m,
      metadata: {
        "sh3-shard": s,
        "sh3-path": encodeURIComponent(n),
        "sh3-uploaded-at": d,
        "sh3-source-tenant": a.sourceTenant ?? "",
        "sh3-sha256": m
      }
    }), await ee(i, { id: Q(), targetId: t.id, shardId: s, path: n, sha256: m, size: w, status: "uploaded", at: d }), { status: "uploaded", sha256: m, size: w };
  } catch (p) {
    const S = p instanceof Error ? p.message : String(p);
    return await ee(i, { id: Q(), targetId: t.id, shardId: s, path: n, sha256: m, size: w, status: "failed", reason: S, at: d }), { status: "failed", reason: S };
  }
}
async function ee(a, t) {
  try {
    await Re(a, t);
  } catch (r) {
    console.warn("[sh3-connector-r2] failed to append upload log:", r);
  }
}
function Oe() {
  const a = /* @__PURE__ */ new Date(), t = (r) => String(r).padStart(2, "0");
  return `${a.getUTCFullYear()}${t(a.getUTCMonth() + 1)}${t(a.getUTCDate())}T${t(a.getUTCHours())}${t(a.getUTCMinutes())}${t(a.getUTCSeconds())}Z`;
}
function Ke(a) {
  return a.slice(0, 8);
}
async function je(a) {
  const t = a.dateOverride ?? Oe(), r = Ke(t), i = typeof a.payload == "string" ? new TextEncoder().encode(a.payload) : a.payload instanceof Uint8Array ? a.payload : new Uint8Array(a.payload), o = await ue(i), s = {};
  for (const [I, T] of Object.entries(a.headers ?? {}))
    s[I.toLowerCase()] = T;
  s.host = a.host, s["x-amz-date"] = t, a.service === "s3" && (s["x-amz-content-sha256"] = o);
  const n = Object.entries(s).map(([I, T]) => [I.toLowerCase(), String(T).trim().replace(/\s+/g, " ")]).sort(([I], [T]) => I < T ? -1 : I > T ? 1 : 0), l = n.map(([I]) => I).join(";"), d = n.map(([I, T]) => `${I}:${T}
`).join(""), u = [
    a.method.toUpperCase(),
    a.path,
    a.query ?? "",
    d,
    l,
    o
  ].join(`
`), w = `${r}/${a.region}/${a.service}/aws4_request`, m = [
    "AWS4-HMAC-SHA256",
    t,
    w,
    await ue(u)
  ].join(`
`), p = await Z(
    new TextEncoder().encode(`AWS4${a.credentials.secretAccessKey}`),
    r
  ), S = await Z(p, a.region), B = await Z(S, a.service), A = await Z(B, "aws4_request"), H = await Z(A, m), j = Array.from(H).map((I) => I.toString(16).padStart(2, "0")).join(""), L = `AWS4-HMAC-SHA256 Credential=${a.credentials.accessKeyId}/${w}, SignedHeaders=${l}, Signature=${j}`, x = { ...s };
  return x.Authorization = L, { url: `https://${a.host}${a.path}${a.query ? `?${a.query}` : ""}`, method: a.method.toUpperCase(), headers: x, body: i };
}
class He extends Error {
  constructor(r, i, o, s) {
    super(r);
    ie(this, "name", "R2Error");
    this.status = i, this.code = o, this.responseText = s;
  }
}
async function Le(a) {
  const t = fetch, r = a.body ?? "", i = await je({
    method: a.method,
    host: a.host,
    path: a.path,
    query: a.query ?? "",
    headers: a.headers,
    payload: r,
    service: a.service,
    region: a.region,
    credentials: a.credentials
  }), o = await t(i.url, {
    method: i.method,
    headers: i.headers,
    body: a.method === "GET" || a.method === "HEAD" ? void 0 : i.body
  });
  if (!o.ok) {
    const s = await o.text().catch(() => ""), n = s.match(/<Code>([^<]+)<\/Code>/);
    throw new He(
      `R2 ${a.method} ${a.path} failed (${o.status})`,
      o.status,
      n == null ? void 0 : n[1],
      s
    );
  }
  return o;
}
function ae(a, t) {
  const r = `${a.accountId}.r2.cloudflarestorage.com`, i = {
    accessKeyId: a.accessKeyId,
    secretAccessKey: a.secretAccessKey
  }, o = `/${a.bucket}`;
  async function s(n, l, d = {}) {
    return Le({
      method: n,
      host: r,
      path: `${o}${l}`,
      query: d.query,
      headers: d.headers,
      body: d.body,
      service: "s3",
      region: a.region,
      credentials: i
    });
  }
  return {
    async headBucket() {
      await s("HEAD", "/");
    },
    async putObject(n) {
      const l = {};
      for (const [u, w] of Object.entries(n.metadata ?? {}))
        l[`x-amz-meta-${u}`] = w;
      return { etag: (await s("PUT", `/${n.key}`, {
        body: n.body,
        headers: {
          "content-type": n.contentType,
          ...l
        }
      })).headers.get("etag") ?? "" };
    },
    async headObject(n) {
      try {
        const l = await s("HEAD", `/${n}`);
        return {
          size: Number(l.headers.get("content-length") ?? 0),
          sha256: l.headers.get("x-amz-meta-sh3-sha256") ?? void 0,
          etag: l.headers.get("etag") ?? "",
          lastModified: l.headers.get("last-modified") ?? void 0
        };
      } catch (l) {
        if (l instanceof Error && "status" in l && l.status === 404)
          return null;
        throw l;
      }
    },
    listObjectsV2({ prefix: n }) {
      return {
        async *[Symbol.asyncIterator]() {
          var d, u, w, m, p;
          let l = null;
          for (; ; ) {
            const S = new URLSearchParams({ "list-type": "2", prefix: n });
            l && S.set("continuation-token", l);
            const A = await (await s("GET", "/", { query: S.toString() })).text();
            for (const j of A.matchAll(/<Contents>([\s\S]*?)<\/Contents>/g)) {
              const L = j[1], x = ((d = /<Key>([^<]*)<\/Key>/.exec(L)) == null ? void 0 : d[1]) ?? "", O = Number(((u = /<Size>([^<]*)<\/Size>/.exec(L)) == null ? void 0 : u[1]) ?? "0"), I = ((w = /<LastModified>([^<]*)<\/LastModified>/.exec(L)) == null ? void 0 : w[1]) ?? "";
              yield { key: x, size: O, lastModified: I };
            }
            if (l = ((m = /<IsTruncated>([^<]*)<\/IsTruncated>/.exec(A)) == null ? void 0 : m[1]) === "true" ? ((p = /<NextContinuationToken>([^<]*)<\/NextContinuationToken>/.exec(A)) == null ? void 0 : p[1]) ?? null : null, !l) break;
          }
        }
      };
    },
    async getObject(n) {
      return (await s("GET", `/${n}`)).text();
    }
  };
}
class te extends Error {
  constructor() {
    super(...arguments);
    ie(this, "name", "MissingCapabilityError");
  }
}
const De = "documents:read not available. Reinstall the shard with the documents:read permission granted (sh3-core 0.9.1+).", Ue = "documents:write not available. Reinstall the shard with the documents:write permission granted (sh3-core 0.9.1+).";
function he(a) {
  return async (t, r) => {
    var s;
    const i = (s = a.browse) == null ? void 0 : s.readFrom;
    if (!i) throw new te(De);
    const o = await i(t, r);
    if (o === null) return null;
    if (typeof o == "string") return o;
    throw new Error(
      `Binary document at ${t}/${r} is not supported in v0.1.0 (deferred).`
    );
  };
}
function Me(a) {
  return async (t, r, i) => {
    var s;
    const o = (s = a.browse) == null ? void 0 : s.writeTo;
    if (!o) throw new te(Ue);
    await o(t, r, i);
  };
}
var Ne = e.from_html('<p class="r2-form__error svelte-h3b7wu"> </p>'), Pe = e.from_html('<button type="button" class="r2-form__hint svelte-h3b7wu">New to Cloudflare R2? Open the setup guide →</button>'), Be = e.from_html('<form class="r2-form svelte-h3b7wu"><label class="svelte-h3b7wu">Label<input type="text" required=""/></label> <label class="svelte-h3b7wu">Cloudflare Account ID<input type="text" required=""/></label> <label class="svelte-h3b7wu">Bucket name<input type="text" required=""/></label> <label class="svelte-h3b7wu">Key prefix (e.g. sh3-laptop/)<input type="text"/></label> <label class="svelte-h3b7wu">Access Key ID<input type="password" required=""/></label> <label class="svelte-h3b7wu">Secret Access Key<input type="password" required=""/></label> <!> <!> <div class="r2-form__actions svelte-h3b7wu"><button type="button">Cancel</button> <button type="submit"> </button></div></form>');
function fe(a, t) {
  var v, _, y, E, D, M;
  e.push(t, !0);
  let r = e.state(e.proxy(((v = t.initial) == null ? void 0 : v.label) ?? "")), i = e.state(e.proxy(((_ = t.initial) == null ? void 0 : _.accountId) ?? "")), o = e.state(e.proxy(((y = t.initial) == null ? void 0 : y.bucket) ?? "")), s = e.state(e.proxy(((E = t.initial) == null ? void 0 : E.keyPrefix) ?? "")), n = e.state(e.proxy(((D = t.initial) == null ? void 0 : D.accessKeyId) ?? "")), l = e.state(e.proxy(((M = t.initial) == null ? void 0 : M.secretAccessKey) ?? "")), d = e.state(!1), u = e.state(null);
  async function w(h) {
    var N, k;
    h.preventDefault(), e.set(u, null), e.set(d, !0);
    const K = {
      id: ((N = t.initial) == null ? void 0 : N.id) ?? _e(),
      createdAt: ((k = t.initial) == null ? void 0 : k.createdAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
      label: e.get(r).trim(),
      accountId: e.get(i).trim(),
      bucket: e.get(o).trim(),
      region: "auto",
      keyPrefix: e.get(s).trim(),
      accessKeyId: e.get(n).trim(),
      secretAccessKey: e.get(l).trim()
    };
    try {
      await ae(K).headBucket(), await t.onSave(K);
    } catch (C) {
      e.set(u, C instanceof Error ? C.message : String(C), !0);
    } finally {
      e.set(d, !1);
    }
  }
  var m = Be(), p = e.child(m), S = e.sibling(e.child(p));
  e.remove_input_defaults(S), e.reset(p);
  var B = e.sibling(p, 2), A = e.sibling(e.child(B));
  e.remove_input_defaults(A), e.reset(B);
  var H = e.sibling(B, 2), j = e.sibling(e.child(H));
  e.remove_input_defaults(j), e.reset(H);
  var L = e.sibling(H, 2), x = e.sibling(e.child(L));
  e.remove_input_defaults(x), e.reset(L);
  var O = e.sibling(L, 2), I = e.sibling(e.child(O));
  e.remove_input_defaults(I), e.reset(O);
  var T = e.sibling(O, 2), f = e.sibling(e.child(T));
  e.remove_input_defaults(f), e.reset(T);
  var U = e.sibling(T, 2);
  {
    var F = (h) => {
      var K = Ne(), N = e.child(K, !0);
      e.reset(K), e.template_effect(() => e.set_text(N, e.get(u))), e.append(h, K);
    };
    e.if(U, (h) => {
      e.get(u) && h(F);
    });
  }
  var $ = e.sibling(U, 2);
  {
    var V = (h) => {
      var K = Pe();
      e.delegated("click", K, function(...N) {
        var k;
        (k = t.onShowGuide) == null || k.apply(this, N);
      }), e.append(h, K);
    };
    e.if($, (h) => {
      t.onShowGuide && h(V);
    });
  }
  var Y = e.sibling($, 2), b = e.child(Y), R = e.sibling(b, 2), c = e.child(R, !0);
  e.reset(R), e.reset(Y), e.reset(m), e.template_effect(() => {
    b.disabled = e.get(d), R.disabled = e.get(d), e.set_text(c, e.get(d) ? "Validating…" : "Save & validate");
  }), e.event("submit", m, w), e.bind_value(S, () => e.get(r), (h) => e.set(r, h)), e.bind_value(A, () => e.get(i), (h) => e.set(i, h)), e.bind_value(j, () => e.get(o), (h) => e.set(o, h)), e.bind_value(x, () => e.get(s), (h) => e.set(s, h)), e.bind_value(I, () => e.get(n), (h) => e.set(n, h)), e.bind_value(f, () => e.get(l), (h) => e.set(l, h)), e.delegated("click", b, function(...h) {
    var K;
    (K = t.onCancel) == null || K.apply(this, h);
  }), e.append(a, m), e.pop();
}
e.delegate(["click"]);
var qe = e.from_html(`<aside class="r2-guide svelte-122r6hr" aria-label="Cloudflare R2 setup guide"><header class="r2-guide__header svelte-122r6hr"><h3 class="svelte-122r6hr">Cloudflare R2 setup</h3> <button class="r2-guide__close svelte-122r6hr" type="button" aria-label="Close guide">×</button></header> <ol class="r2-guide__steps svelte-122r6hr"><li class="svelte-122r6hr"><strong>Sign in to Cloudflare.</strong> Go to <a href="https://dash.cloudflare.com" target="_blank" rel="noopener" class="svelte-122r6hr">dash.cloudflare.com</a>.
      R2 requires a payment method to enable, but the free tier covers 10 GB of storage and 1M Class A / 10M Class B ops per month.</li> <li class="svelte-122r6hr"><strong>Enable R2 and create a bucket.</strong> From the left sidebar, click <em>R2</em>. If R2 isn't enabled, click <em>Enable R2</em> and accept the terms.
      Then click <em>Create bucket</em>, give it a name (e.g. <code class="svelte-122r6hr">sh3-backups</code>), and pick a location hint close to you.</li> <li class="svelte-122r6hr"><strong>Copy your Account ID.</strong> On the R2 overview page, the right sidebar shows <em>Account ID</em>. Paste it into the form's Account ID field.</li> <li class="svelte-122r6hr"><strong>Create an API token.</strong> Top right of the R2 page: <em>Manage R2 API Tokens</em> → <em>Create API token</em>.
      Name it (e.g. <code class="svelte-122r6hr">sh3-connector-r2</code>), choose <em>Object Read &amp; Write</em>, scope it to your bucket.
      After creating, copy the <em>Access Key ID</em> and <em>Secret Access Key</em> — they are shown <strong>once</strong> — into the form.</li> <li class="svelte-122r6hr"><strong>Configure CORS on the bucket.</strong> Open your bucket → <em>Settings</em> → <em>CORS Policy</em> → <em>Edit CORS policy</em> → paste: <pre class="svelte-122r6hr"> </pre> Without this, the browser blocks uploads from this SH3 origin.</li> <li class="svelte-122r6hr"><strong>Fill in the form and save.</strong> Back in the left column: fill Label + Account ID + Bucket + Key prefix (optional) + Access Key + Secret. <em>Save &amp; validate</em> issues a signed <code class="svelte-122r6hr">HeadBucket</code> — a green result means credentials and CORS are both working.</li></ol></aside>`);
function ze(a, t) {
  const i = `[
  {
    "AllowedOrigins": ["${typeof location < "u" ? location.origin : "<SH3 origin>"}"],
    "AllowedMethods": ["PUT", "GET", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"]
  }
]`;
  var o = qe(), s = e.child(o), n = e.sibling(e.child(s), 2);
  e.reset(s);
  var l = e.sibling(s, 2), d = e.sibling(e.child(l), 8), u = e.sibling(e.child(d), 8), w = e.child(u, !0);
  e.reset(u), e.next(), e.reset(d), e.next(2), e.reset(l), e.reset(o), e.template_effect(() => e.set_text(w, i)), e.delegated("click", n, function(...m) {
    var p;
    (p = t.onClose) == null || p.apply(this, m);
  }), e.append(a, o);
}
e.delegate(["click"]);
var Fe = e.from_html('<button type="button">Help</button>'), $e = e.from_html('<button type="button">Add target</button>'), Ge = e.from_html("<p>Loading…</p>"), Ve = e.from_html(`<p class="muted svelte-16gewpp">No targets yet. Click "Add target" to configure one, or "Help" if you're new to Cloudflare R2.</p>`), We = e.from_html('<tr><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"><button type="button">Edit</button> <button type="button" class="danger svelte-16gewpp">Delete</button></td></tr>'), Je = e.from_html('<table class="svelte-16gewpp"><thead><tr><th class="svelte-16gewpp">Label</th><th class="svelte-16gewpp">Bucket</th><th class="svelte-16gewpp">Prefix</th><th class="svelte-16gewpp"></th></tr></thead><tbody></tbody></table>'), Ye = e.from_html('<div class="r2-targets__guide"><!></div>'), Xe = e.from_html('<div><div class="r2-targets__main"><header class="svelte-16gewpp"><h2>R2 Targets</h2> <div class="r2-targets__actions svelte-16gewpp"><!> <!></div></header> <!></div> <!></div>');
function Ze(a, t) {
  e.push(t, !0);
  let r = e.state(null), i = e.state(!1);
  async function o(f) {
    await we(t.rt.docs, f), await t.rt.refreshTargets(), e.set(r, null);
  }
  async function s(f) {
    confirm("Delete this target? Existing R2 objects are not removed.") && (await xe(t.rt.docs, f), await t.rt.refreshTargets());
  }
  function n() {
    e.set(i, !0);
  }
  function l() {
    e.set(i, !1);
  }
  var d = Xe();
  let u;
  var w = e.child(d), m = e.child(w), p = e.sibling(e.child(m), 2), S = e.child(p);
  {
    var B = (f) => {
      var U = Fe();
      e.delegated("click", U, n), e.append(f, U);
    };
    e.if(S, (f) => {
      e.get(i) || f(B);
    });
  }
  var A = e.sibling(S, 2);
  {
    var H = (f) => {
      var U = $e();
      e.delegated("click", U, () => e.set(r, "new")), e.append(f, U);
    };
    e.if(A, (f) => {
      e.get(r) === null && f(H);
    });
  }
  e.reset(p), e.reset(m);
  var j = e.sibling(m, 2);
  {
    var L = (f) => {
      fe(f, {
        onSave: o,
        onCancel: () => e.set(r, null),
        onShowGuide: n
      });
    }, x = (f) => {
      fe(f, {
        get initial() {
          return e.get(r);
        },
        onSave: o,
        onCancel: () => e.set(r, null),
        onShowGuide: n
      });
    }, O = (f) => {
      var U = e.comment(), F = e.first_child(U);
      {
        var $ = (b) => {
          var R = Ge();
          e.append(b, R);
        }, V = (b) => {
          var R = Ve();
          e.append(b, R);
        }, Y = (b) => {
          var R = Je(), c = e.sibling(e.child(R));
          e.each(c, 21, () => t.rt.targets, (v) => v.id, (v, _) => {
            var y = We(), E = e.child(y), D = e.child(E, !0);
            e.reset(E);
            var M = e.sibling(E), h = e.child(M, !0);
            e.reset(M);
            var K = e.sibling(M), N = e.child(K, !0);
            e.reset(K);
            var k = e.sibling(K), C = e.child(k), g = e.sibling(C, 2);
            e.reset(k), e.reset(y), e.template_effect(() => {
              e.set_text(D, e.get(_).label), e.set_text(h, e.get(_).bucket), e.set_text(N, e.get(_).keyPrefix || "(none)");
            }), e.delegated("click", C, () => e.set(r, e.get(_), !0)), e.delegated("click", g, () => s(e.get(_).id)), e.append(v, y);
          }), e.reset(c), e.reset(R), e.append(b, R);
        };
        e.if(F, (b) => {
          t.rt.targetsLoaded ? t.rt.targets.length === 0 ? b(V, 1) : b(Y, -1) : b($);
        });
      }
      e.append(f, U);
    };
    e.if(j, (f) => {
      e.get(r) === "new" ? f(L) : e.get(r) && typeof e.get(r) != "string" ? f(x, 1) : f(O, -1);
    });
  }
  e.reset(w);
  var I = e.sibling(w, 2);
  {
    var T = (f) => {
      var U = Ye(), F = e.child(U);
      ze(F, { onClose: l }), e.reset(U), e.append(f, U);
    };
    e.if(I, (f) => {
      e.get(i) && f(T);
    });
  }
  e.reset(d), e.template_effect(() => u = e.set_class(d, 1, "r2-targets svelte-16gewpp", null, u, { "r2-targets--split": e.get(i) })), e.append(a, d), e.pop();
}
e.delegate(["click"]);
async function Qe(a) {
  const t = await a.list(), r = a.scope.pathPrefix ?? "", i = t.filter(
    (s) => s.shardId === a.scope.shardId && s.path.startsWith(r)
  ), o = { total: i.length, uploaded: 0, skipped: 0, failed: 0 };
  for (let s = 0; s < i.length; s++) {
    const n = await a.onItem(i[s], s, i.length);
    n === "uploaded" ? o.uploaded++ : n === "skipped-unchanged" ? o.skipped++ : o.failed++;
  }
  return o;
}
var et = e.from_html("<li> </li>"), tt = e.from_html("<details><summary> </summary> <ul></ul></details>"), at = e.from_html('<div class="r2-progress svelte-putn2e"><div class="r2-progress__label"><!></div> <div class="r2-progress__bar svelte-putn2e"><div class="r2-progress__fill svelte-putn2e"></div></div> <div class="r2-progress__counts svelte-putn2e"> </div> <!></div>');
function rt(a, t) {
  e.push(t, !0);
  const r = e.derived(() => t.p.total === 0 ? 0 : Math.round(t.p.processed / t.p.total * 100));
  var i = e.comment(), o = e.first_child(i);
  {
    var s = (n) => {
      var l = at(), d = e.child(l), u = e.child(d);
      {
        var w = (x) => {
          var O = e.text();
          e.template_effect(() => e.set_text(O, `Running: ${t.p.currentLabel ?? ""}`)), e.append(x, O);
        }, m = (x) => {
          var O = e.text("Done.");
          e.append(x, O);
        };
        e.if(u, (x) => {
          t.p.running ? x(w) : x(m, -1);
        });
      }
      e.reset(d);
      var p = e.sibling(d, 2), S = e.child(p);
      let B;
      e.reset(p);
      var A = e.sibling(p, 2), H = e.child(A);
      e.reset(A);
      var j = e.sibling(A, 2);
      {
        var L = (x) => {
          var O = tt(), I = e.child(O), T = e.child(I);
          e.reset(I);
          var f = e.sibling(I, 2);
          e.each(f, 21, () => t.p.errors, e.index, (U, F) => {
            var $ = et(), V = e.child($, !0);
            e.reset($), e.template_effect(() => e.set_text(V, e.get(F))), e.append(U, $);
          }), e.reset(f), e.reset(O), e.template_effect(() => e.set_text(T, `${t.p.errors.length ?? ""} error${t.p.errors.length === 1 ? "" : "s"}`)), e.append(x, O);
        };
        e.if(j, (x) => {
          t.p.errors.length > 0 && x(L);
        });
      }
      e.reset(l), e.template_effect(() => {
        B = e.set_style(S, "", B, { width: `${e.get(r) ?? ""}%` }), e.set_text(H, `${t.p.processed ?? ""}/${t.p.total ?? ""} — uploaded ${t.p.uploaded ?? ""}, skipped ${t.p.skipped ?? ""}, failed ${t.p.failed ?? ""}`);
      }), e.append(n, l);
    };
    e.if(o, (n) => {
      (t.p.running || t.p.total > 0) && n(s);
    });
  }
  e.append(a, i), e.pop();
}
var st = e.from_html("<p>No R2 targets configured. Add one in the Targets tab.</p>"), nt = e.from_html("<option> </option>"), ot = e.from_html("<option> </option>"), it = e.from_html('<p class="muted svelte-8kfsmf">No uploads yet.</p>'), lt = e.from_html('<span class="reason"> </span>'), ct = e.from_html('<li><span class="ts"> </span> <span class="path"> </span> <span class="status"> </span> <!></li>'), dt = e.from_html('<ul class="r2-log svelte-8kfsmf"></ul>'), ut = e.from_html('<label class="svelte-8kfsmf">Target <select class="svelte-8kfsmf"></select></label> <label class="svelte-8kfsmf">Shard <select class="svelte-8kfsmf"></select></label> <label class="svelte-8kfsmf">Path prefix (optional) <input type="text" placeholder="e.g. journal/"/></label> <button> </button> <!> <h3>Recent activity</h3> <!>', 1), gt = e.from_html('<div class="r2-backup svelte-8kfsmf"><h2>Back up to R2</h2> <!></div>');
function ht(a, t) {
  e.push(t, !0);
  let r = e.prop(t, "rt", 7), i = e.state(""), o = e.state(""), s = e.state(""), n = e.state(e.proxy([])), l = e.state(e.proxy([]));
  e.user_effect(() => {
    r().targets.length > 0 && !e.get(i) && e.set(i, r().targets[0].id, !0);
  });
  async function d() {
    r().ctx.browse && (e.set(n, await r().ctx.browse.listShards(), !0), !e.get(o) && e.get(n).length > 0 && e.set(o, e.get(n)[0], !0));
  }
  e.user_effect(() => {
    d();
  });
  async function u() {
    e.set(l, await Ee(r().docs, 50), !0);
  }
  e.user_effect(() => {
    u();
  });
  async function w() {
    const A = r().targets.find((H) => H.id === e.get(i));
    if (!(!A || !r().ctx.browse)) {
      if (!e.get(o)) {
        alert("Pick a shard to back up.");
        return;
      }
      r().progress.running = !0, r().progress.currentLabel = "", r().progress.total = 0, r().progress.processed = 0, r().progress.uploaded = 0, r().progress.skipped = 0, r().progress.failed = 0, r().progress.errors = [];
      try {
        const H = ae(A), j = he(r().ctx), L = r().ctx.browse;
        await Qe({
          list: async () => (await L.listDocuments()).map((O) => ({ shardId: O.shardId, path: O.path })),
          scope: {
            shardId: e.get(o),
            pathPrefix: e.get(s) || void 0
          },
          onItem: async (x, O, I) => {
            r().progress.currentLabel = `${x.shardId}/${x.path}`, r().progress.total = I;
            const T = await ve({
              target: A,
              client: H,
              logHandle: r().docs,
              readForeign: j,
              shardId: x.shardId,
              path: x.path,
              sourceTenant: r().ctx.tenantId
            });
            return r().progress.processed++, T.status === "uploaded" ? r().progress.uploaded++ : T.status === "skipped-unchanged" ? r().progress.skipped++ : (r().progress.failed++, T.reason && r().progress.errors.push(`${x.path}: ${T.reason}`)), T.status;
          }
        });
      } finally {
        r().progress.running = !1, await u();
      }
    }
  }
  var m = gt(), p = e.sibling(e.child(m), 2);
  {
    var S = (A) => {
      var H = st();
      e.append(A, H);
    }, B = (A) => {
      var H = ut(), j = e.first_child(H), L = e.sibling(e.child(j));
      e.each(L, 21, () => r().targets, (b) => b.id, (b, R) => {
        var c = nt(), v = e.child(c);
        e.reset(c);
        var _ = {};
        e.template_effect(() => {
          e.set_text(v, `${e.get(R).label ?? ""} (${e.get(R).bucket ?? ""})`), _ !== (_ = e.get(R).id) && (c.value = (c.__value = e.get(R).id) ?? "");
        }), e.append(b, c);
      }), e.reset(L), e.reset(j);
      var x = e.sibling(j, 2), O = e.sibling(e.child(x));
      e.each(O, 21, () => e.get(n), e.index, (b, R) => {
        var c = ot(), v = e.child(c, !0);
        e.reset(c);
        var _ = {};
        e.template_effect(() => {
          e.set_text(v, e.get(R)), _ !== (_ = e.get(R)) && (c.value = (c.__value = e.get(R)) ?? "");
        }), e.append(b, c);
      }), e.reset(O), e.reset(x);
      var I = e.sibling(x, 2), T = e.sibling(e.child(I));
      e.remove_input_defaults(T), e.reset(I);
      var f = e.sibling(I, 2), U = e.child(f, !0);
      e.reset(f);
      var F = e.sibling(f, 2);
      rt(F, {
        get p() {
          return r().progress;
        }
      });
      var $ = e.sibling(F, 4);
      {
        var V = (b) => {
          var R = it();
          e.append(b, R);
        }, Y = (b) => {
          var R = dt();
          e.each(R, 21, () => e.get(l), e.index, (c, v) => {
            var _ = ct(), y = e.child(_), E = e.child(y, !0);
            e.reset(y);
            var D = e.sibling(y, 2), M = e.child(D);
            e.reset(D);
            var h = e.sibling(D, 2), K = e.child(h, !0);
            e.reset(h);
            var N = e.sibling(h, 2);
            {
              var k = (C) => {
                var g = lt(), P = e.child(g);
                e.reset(g), e.template_effect(() => e.set_text(P, `— ${e.get(v).reason ?? ""}`)), e.append(C, g);
              };
              e.if(N, (C) => {
                e.get(v).reason && C(k);
              });
            }
            e.reset(_), e.template_effect(() => {
              e.set_class(_, 1, `status-${e.get(v).status}`, "svelte-8kfsmf"), e.set_text(E, e.get(v).at), e.set_text(M, `${e.get(v).shardId ?? ""}/${e.get(v).path ?? ""}`), e.set_text(K, e.get(v).status);
            }), e.append(c, _);
          }), e.reset(R), e.append(b, R);
        };
        e.if($, (b) => {
          e.get(l).length === 0 ? b(V) : b(Y, -1);
        });
      }
      e.template_effect(() => {
        f.disabled = r().progress.running, e.set_text(U, r().progress.running ? "Running…" : "Back up now");
      }), e.bind_select_value(L, () => e.get(i), (b) => e.set(i, b)), e.bind_select_value(O, () => e.get(o), (b) => e.set(o, b)), e.bind_value(T, () => e.get(s), (b) => e.set(s, b)), e.delegated("click", f, w), e.append(A, H);
    };
    e.if(p, (A) => {
      r().targets.length === 0 ? A(S) : A(B, -1);
    });
  }
  e.reset(m), e.append(a, m), e.pop();
}
e.delegate(["click"]);
function pt(a) {
  const t = a.lastIndexOf(".");
  if (t < 0) return;
  const r = a.lastIndexOf("/");
  if (!(t < r))
    return a.slice(t);
}
function ft(a) {
  const t = `${a.shardId}/${a.path}`, r = {
    origin: "local",
    version: a.localVersion,
    at: a.localAt,
    content: a.localContent
  }, i = {
    origin: `r2:${a.targetLabel}`,
    version: 0,
    at: a.incomingAt,
    content: a.incomingContent
  }, o = {
    id: t,
    label: t,
    branches: [r, i],
    meta: { remoteKey: a.remoteKey, shardId: a.shardId, path: a.path }
  }, s = pt(a.path);
  return s !== void 0 && (o.extension = s), o;
}
async function vt(a, t) {
  if (t.length === 0) return [];
  const r = t.map(ft), i = new Map(
    t.map((n) => [`${n.shardId}/${n.path}`, n])
  );
  let o;
  try {
    o = await a.resolve(r, { title: "Resolve R2 import conflicts" });
  } catch {
    return "cancelled";
  }
  if (o.status === "cancelled") return "cancelled";
  const s = [];
  for (const n of o.choices) {
    const l = i.get(n.itemId);
    if (!l) continue;
    const d = n.chosen.origin === "local" ? "local" : "incoming";
    s.push({
      shardId: l.shardId,
      path: l.path,
      choice: d,
      remoteKey: l.remoteKey
    });
  }
  for (const n of o.skipped) {
    const l = i.get(n);
    l && s.push({
      shardId: l.shardId,
      path: l.path,
      choice: "skipped",
      remoteKey: l.remoteKey
    });
  }
  return s;
}
var mt = e.from_html('<span class="badge svelte-8cyv1p" title="Already exists locally — unchecked by default to protect local copy">local</span>'), bt = e.from_html('<tr><td class="svelte-8cyv1p"><input type="checkbox" class="shell-base-check"/></td><td class="svelte-8cyv1p"> <!></td><td class="svelte-8cyv1p"> </td><td class="svelte-8cyv1p"> </td></tr>'), _t = e.from_html('<table class="r2-tree svelte-8cyv1p"><thead><tr><th class="svelte-8cyv1p"></th><th class="svelte-8cyv1p">Key</th><th class="svelte-8cyv1p">Size</th><th class="svelte-8cyv1p">Modified</th></tr></thead><tbody></tbody></table>');
function yt(a, t) {
  e.push(t, !0);
  var r = _t(), i = e.sibling(e.child(r));
  e.each(i, 21, () => t.nodes, (o) => o.path, (o, s) => {
    var n = bt(), l = e.child(n), d = e.child(l);
    e.remove_input_defaults(d), e.reset(l);
    var u = e.sibling(l), w = e.child(u), m = e.sibling(w);
    {
      var p = (j) => {
        var L = mt();
        e.append(j, L);
      };
      e.if(m, (j) => {
        e.get(s).existsLocal && j(p);
      });
    }
    e.reset(u);
    var S = e.sibling(u), B = e.child(S, !0);
    e.reset(S);
    var A = e.sibling(S), H = e.child(A, !0);
    e.reset(A), e.reset(n), e.template_effect(
      (j) => {
        e.set_class(n, 1, e.clsx(e.get(s).existsLocal ? "exists" : ""), "svelte-8cyv1p"), e.set_checked(d, j), e.set_text(w, `${e.get(s).path ?? ""} `), e.set_text(B, e.get(s).size), e.set_text(H, e.get(s).lastModified);
      },
      [() => t.selected.has(e.get(s).path)]
    ), e.delegated("change", d, () => t.onToggle(e.get(s).path)), e.append(o, n);
  }), e.reset(i), e.reset(r), e.append(a, r), e.pop();
}
e.delegate(["change"]);
var wt = e.from_html(`<div class="warn svelte-1kdudbk">Import requires the <code>documents:write</code> permission (sh3-core 0.9.1+).
      Reinstall the shard and grant this permission when prompted.</div>`), xt = e.from_html("<p>No R2 targets configured. Add one in the Targets tab.</p>"), kt = e.from_html("<option> </option>"), It = e.from_html('<p> </p> <!> <div class="actions svelte-1kdudbk"><button> </button> <button> </button> <button> </button></div>', 1), St = e.from_html("<li> </li>"), At = e.from_html("<details><summary>Errors</summary><ul></ul></details>"), Ct = e.from_html('<div class="summary svelte-1kdudbk"> <!></div>'), Tt = e.from_html('<label class="svelte-1kdudbk">Target <select></select></label> <button> </button> <!> <!>', 1), Rt = e.from_html('<div class="r2-import svelte-1kdudbk"><h2>Import from R2</h2> <!> <!></div>');
function Et(a, t) {
  var b, R;
  e.push(t, !0);
  let r = e.state("");
  function i() {
    return {
      imported: 0,
      resolvedLocal: 0,
      resolvedIncoming: 0,
      skipped: 0,
      cancelled: 0,
      failed: 0,
      errors: []
    };
  }
  let o = e.state(e.proxy([])), s = e.state(e.proxy(/* @__PURE__ */ new Set())), n = e.state(!1), l = e.state(!1), d = e.state(!1), u = e.state(null);
  const w = typeof ((R = (b = de) == null ? void 0 : b.conflicts) == null ? void 0 : R.resolve) == "function", m = e.derived(() => e.get(o).filter((c) => e.get(s).has(c.path))), p = e.derived(() => e.get(m).filter((c) => !c.existsLocal)), S = e.derived(() => e.get(m).filter((c) => c.existsLocal && !c.binaryUnsupported)), B = e.derived(() => e.get(m).filter((c) => c.existsLocal && c.binaryUnsupported)), A = e.derived(() => e.get(p).length > 0 && e.get(S).length === 0 && e.get(B).length === 0), H = e.derived(() => w && e.get(S).length > 0 && e.get(p).length === 0), j = e.derived(() => w && e.get(p).length > 0 && e.get(S).length > 0);
  e.user_effect(() => {
    t.rt.targets.length > 0 && !e.get(r) && e.set(r, t.rt.targets[0].id, !0);
  });
  async function L() {
    const c = t.rt.targets.find((v) => v.id === e.get(r));
    if (!(!c || !t.rt.ctx.browse)) {
      e.set(u, null), e.set(n, !0);
      try {
        const v = ae(c), _ = he(t.rt.ctx), y = t.rt.ctx.browse.statusFrom, E = /* @__PURE__ */ new Set();
        for (const h of await t.rt.ctx.browse.listDocuments()) E.add(`${h.shardId}/${h.path}`);
        const D = [], M = /* @__PURE__ */ new Set();
        for await (const h of v.listObjectsV2({ prefix: c.keyPrefix })) {
          const K = h.key.slice(c.keyPrefix.length), N = K.indexOf("/");
          if (N < 0) continue;
          const k = K.slice(0, N), C = K.slice(N + 1), g = E.has(`${k}/${C}`);
          let P, q, W = !1;
          if (g) {
            try {
              P = await _(k, C) ?? "";
            } catch (z) {
              if (z instanceof te) {
                e.set(d, !0), e.set(n, !1);
                return;
              }
              W = !0;
            }
            if (y)
              try {
                const z = await y(k, C);
                z && (q = z);
              } catch {
              }
          }
          D.push({
            path: h.key,
            size: h.size,
            lastModified: h.lastModified,
            existsLocal: g,
            shardId: k,
            docPath: C,
            localContent: P,
            localStatus: q,
            binaryUnsupported: W || void 0
          }), g || M.add(h.key);
        }
        e.set(o, D, !0), e.set(s, M, !0);
      } finally {
        e.set(n, !1);
      }
    }
  }
  function x(c) {
    e.get(s).has(c) ? e.get(s).delete(c) : e.get(s).add(c), e.set(s, new Set(e.get(s)), !0);
  }
  async function O(c, v, _, y) {
    for (const E of y)
      try {
        const D = await c.getObject(E.path);
        await v(E.shardId, E.docPath, D), _.imported++;
      } catch (D) {
        if (D instanceof te)
          return e.set(d, !0), !1;
        _.failed++, _.errors.push(`${E.path}: ${D instanceof Error ? D.message : String(D)}`);
      }
    return !0;
  }
  async function I(c, v, _, y, E, D) {
    var N, k, C;
    for (const g of D)
      y.failed++, y.errors.push(`${g.path}: binary not supported`);
    if (E.length === 0 || !((N = de) != null && N.conflicts)) return !0;
    const M = [];
    for (const g of E)
      try {
        const P = await v.getObject(g.path), q = Date.parse(g.lastModified);
        M.push({
          shardId: g.shardId,
          path: g.docPath,
          localContent: g.localContent ?? "",
          localVersion: ((k = g.localStatus) == null ? void 0 : k.version) ?? 1,
          localAt: ((C = g.localStatus) == null ? void 0 : C.lastSyncedAt) ?? 0,
          incomingContent: P,
          incomingAt: Number.isFinite(q) ? q : Date.now(),
          targetLabel: c.label,
          remoteKey: g.path
        });
      } catch (P) {
        y.failed++, y.errors.push(`${g.path}: ${P instanceof Error ? P.message : String(P)}`);
      }
    if (M.length === 0) return !0;
    const h = await vt(de.conflicts, M);
    if (h === "cancelled")
      return y.cancelled += M.length, !0;
    const K = new Map(M.map((g) => [g.remoteKey, g]));
    for (const g of h)
      if (g.choice === "local")
        y.resolvedLocal++;
      else if (g.choice === "skipped")
        y.skipped++;
      else {
        const P = K.get(g.remoteKey);
        if (!P) continue;
        try {
          await _(g.shardId, g.path, P.incomingContent), y.resolvedIncoming++;
        } catch (q) {
          if (q instanceof te)
            return e.set(d, !0), !1;
          y.failed++, y.errors.push(`${g.shardId}/${g.path}: ${q instanceof Error ? q.message : String(q)}`);
        }
      }
    return !0;
  }
  async function T(c) {
    const v = t.rt.targets.find((y) => y.id === e.get(r));
    if (!v) return;
    e.set(u, null), e.set(l, !0);
    const _ = i();
    try {
      const y = ae(v), E = Me(t.rt.ctx);
      if ((c === "new" || c === "mixed") && !await O(y, E, _, e.get(p)))
        return;
      (c === "updated" || c === "mixed") && await I(v, y, E, _, e.get(S), c === "updated" ? e.get(B) : []);
    } finally {
      e.set(l, !1), e.set(u, _, !0);
    }
  }
  var f = Rt(), U = e.sibling(e.child(f), 2);
  {
    var F = (c) => {
      var v = wt();
      e.append(c, v);
    };
    e.if(U, (c) => {
      e.get(d) && c(F);
    });
  }
  var $ = e.sibling(U, 2);
  {
    var V = (c) => {
      var v = xt();
      e.append(c, v);
    }, Y = (c) => {
      var v = Tt(), _ = e.first_child(v), y = e.sibling(e.child(_));
      e.each(y, 21, () => t.rt.targets, (k) => k.id, (k, C) => {
        var g = kt(), P = e.child(g);
        e.reset(g);
        var q = {};
        e.template_effect(() => {
          e.set_text(P, `${e.get(C).label ?? ""} (${e.get(C).bucket ?? ""})`), q !== (q = e.get(C).id) && (g.value = (g.__value = e.get(C).id) ?? "");
        }), e.append(k, g);
      }), e.reset(y), e.reset(_);
      var E = e.sibling(_, 2), D = e.child(E, !0);
      e.reset(E);
      var M = e.sibling(E, 2);
      {
        var h = (k) => {
          var C = It(), g = e.first_child(C), P = e.child(g);
          e.reset(g);
          var q = e.sibling(g, 2);
          yt(q, {
            get nodes() {
              return e.get(o);
            },
            get selected() {
              return e.get(s);
            },
            onToggle: x
          });
          var W = e.sibling(q, 2), z = e.child(W), re = e.child(z, !0);
          e.reset(z);
          var X = e.sibling(z, 2), ne = e.child(X, !0);
          e.reset(X);
          var J = e.sibling(X, 2), oe = e.child(J, !0);
          e.reset(J), e.reset(W), e.template_effect(() => {
            e.set_text(P, `${e.get(o).length ?? ""} object${e.get(o).length === 1 ? "" : "s"} — ${e.get(s).size ?? ""} selected`), z.disabled = e.get(l) || !e.get(A), e.set_text(re, e.get(l) ? "Importing…" : `Import New (${e.get(p).length})`), X.disabled = e.get(l) || !e.get(H), e.set_attribute(X, "title", w ? "" : "Requires sh3-core 0.10.4+"), e.set_text(ne, e.get(l) ? "Importing…" : `Import Updated (${e.get(S).length})`), J.disabled = e.get(l) || !e.get(j), e.set_attribute(J, "title", w ? "" : "Requires sh3-core 0.10.4+"), e.set_text(oe, e.get(l) ? "Importing…" : "Import Selected");
          }), e.delegated("click", z, () => T("new")), e.delegated("click", X, () => T("updated")), e.delegated("click", J, () => T("mixed")), e.append(k, C);
        };
        e.if(M, (k) => {
          e.get(o).length > 0 && k(h);
        });
      }
      var K = e.sibling(M, 2);
      {
        var N = (k) => {
          var C = Ct(), g = e.child(C), P = e.sibling(g);
          {
            var q = (W) => {
              var z = At(), re = e.sibling(e.child(z));
              e.each(re, 21, () => e.get(u).errors, e.index, (X, ne) => {
                var J = St(), oe = e.child(J, !0);
                e.reset(J), e.template_effect(() => e.set_text(oe, e.get(ne))), e.append(X, J);
              }), e.reset(re), e.reset(z), e.append(W, z);
            };
            e.if(P, (W) => {
              e.get(u).errors.length > 0 && W(q);
            });
          }
          e.reset(C), e.template_effect(() => e.set_text(g, `Imported: ${e.get(u).imported ?? ""}
        · Kept local: ${e.get(u).resolvedLocal ?? ""}
        · Applied remote: ${e.get(u).resolvedIncoming ?? ""}
        · Skipped: ${e.get(u).skipped ?? ""}
        · Cancelled: ${e.get(u).cancelled ?? ""}
        · Failed: ${e.get(u).failed ?? ""} `)), e.append(k, C);
        };
        e.if(K, (k) => {
          e.get(u) && k(N);
        });
      }
      e.template_effect(() => {
        E.disabled = e.get(n), e.set_text(D, e.get(n) ? "Scanning…" : "Scan bucket");
      }), e.bind_select_value(y, () => e.get(r), (k) => e.set(r, k)), e.delegated("click", E, L), e.append(c, v);
    };
    e.if($, (c) => {
      t.rt.targets.length === 0 ? c(V) : c(Y, -1);
    });
  }
  e.reset(f), e.append(a, f), e.pop();
}
e.delegate(["click"]);
const Ot = "sh3-file-explorer.selectionAction";
let G = null;
const Dt = {
  manifest: {
    id: "sh3-connector-r2",
    label: "R2 Backup",
    views: [
      { id: "sh3-connector-r2-targets", label: "Targets" },
      { id: "sh3-connector-r2-backup", label: "Backup" },
      { id: "sh3-connector-r2-import", label: "Import" }
    ],
    permissions: ["documents:browse", "documents:read", "documents:write"]
  },
  async activate(a) {
    G = ke(a), G.refreshTargets(), a.registerView("sh3-connector-r2-targets", {
      mount(t) {
        const r = le(Ze, { target: t, props: { rt: G } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.registerView("sh3-connector-r2-backup", {
      mount(t) {
        const r = le(ht, { target: t, props: { rt: G } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.registerView("sh3-connector-r2-import", {
      mount(t) {
        const r = le(Et, { target: t, props: { rt: G } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.contributions.register(Ot, {
      id: "sh3-connector-r2:backup",
      label: "Back up to R2",
      appliesTo: () => ((G == null ? void 0 : G.targets.length) ?? 0) > 0,
      onInvoke: async (t) => {
        if (!G) return;
        const r = G.targets;
        if (r.length === 0) {
          alert("No R2 targets configured. Open the R2 Backup app to add one.");
          return;
        }
        const i = r.length === 1 ? r[0] : r.find((d) => d.label === window.prompt(
          `Target label (${r.map((u) => u.label).join(", ")}):`,
          r[0].label
        ));
        if (!i) return;
        const o = ae(i), s = he(a), n = await ve({
          target: i,
          client: o,
          logHandle: G.docs,
          readForeign: s,
          shardId: t.shardId,
          path: t.path,
          sourceTenant: a.tenantId
        }), l = n.status === "uploaded" ? "Uploaded" : n.status === "skipped-unchanged" ? "Already up to date" : `Failed: ${n.reason ?? "unknown"}`;
        alert(`${l}
${t.shardId}/${t.path}`);
      },
      kind: "secondary"
    });
  },
  deactivate() {
    G = null;
  }
}, Ut = {
  manifest: {
    id: "sh3-connector-r2-app",
    label: "R2 Backup",
    requiredShards: ["sh3-connector-r2"],
    layoutVersion: 2,
    permissions: ["documents:browse"]
  },
  initialLayout: {
    type: "tabs",
    activeTab: 0,
    tabs: [
      { slotId: "sh3-connector-r2.targets", viewId: "sh3-connector-r2-targets", label: "Targets" },
      { slotId: "sh3-connector-r2.backup", viewId: "sh3-connector-r2-backup", label: "Backup" },
      { slotId: "sh3-connector-r2.import", viewId: "sh3-connector-r2-import", label: "Import" }
    ]
  }
};
export {
  Ut as app,
  Dt as shard
};
