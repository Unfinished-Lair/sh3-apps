/* sh3-css-inline: injected styles */
(function(){
  const s=document.createElement("style");
  s.textContent=".r2-form.svelte-h3b7wu{display:flex;flex-direction:column;gap:8px;max-width:480px;padding:12px}.r2-form.svelte-h3b7wu label:where(.svelte-h3b7wu){display:flex;flex-direction:column;gap:2px;font-size:.9em}.r2-form__error.svelte-h3b7wu{color:#e66;margin:0}.r2-form__hint.svelte-h3b7wu{align-self:flex-start;background:transparent;border:none;color:var(--sh3-accent, #4a9eff);padding:0;font:inherit;cursor:pointer;text-decoration:underline;text-underline-offset:2px}.r2-form__actions.svelte-h3b7wu{display:flex;gap:8px;justify-content:flex-end;margin-top:8px}.r2-guide.svelte-122r6hr{background:var(--sh3-surface, #1a1a1a);border:1px solid var(--sh3-border, #2a2a2a);border-radius:4px;padding:12px 16px;overflow:auto;max-height:100%}.r2-guide__header.svelte-122r6hr{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px}.r2-guide__header.svelte-122r6hr h3:where(.svelte-122r6hr){margin:0;font-size:1em}.r2-guide__close.svelte-122r6hr{background:transparent;color:inherit;border:none;font-size:1.4em;line-height:1;cursor:pointer;padding:0 4px}.r2-guide__close.svelte-122r6hr:hover{color:var(--sh3-accent, #4a9eff)}.r2-guide__steps.svelte-122r6hr{padding-left:1.25em;display:flex;flex-direction:column;gap:10px;font-size:.9em}.r2-guide__steps.svelte-122r6hr li:where(.svelte-122r6hr){line-height:1.45}.r2-guide__steps.svelte-122r6hr code:where(.svelte-122r6hr){font-size:.9em;background:var(--sh3-bg, #111);padding:1px 4px;border-radius:2px}.r2-guide__steps.svelte-122r6hr a:where(.svelte-122r6hr){color:var(--sh3-accent, #4a9eff)}.r2-guide__steps.svelte-122r6hr pre:where(.svelte-122r6hr){font-size:.82em;background:var(--sh3-bg, #111);padding:8px;border-radius:3px;overflow:auto;margin:6px 0 0}.r2-targets.svelte-16gewpp{padding:12px}.r2-targets--split.svelte-16gewpp{display:grid;grid-template-columns:1fr 1fr;gap:16px;align-items:start}@media(max-width:720px){.r2-targets--split.svelte-16gewpp{grid-template-columns:1fr}}header.svelte-16gewpp{display:flex;justify-content:space-between;align-items:center}.r2-targets__actions.svelte-16gewpp{display:flex;gap:6px}table.svelte-16gewpp{width:100%;border-collapse:collapse;margin-top:8px}th.svelte-16gewpp,td.svelte-16gewpp{text-align:left;padding:6px 8px;border-bottom:1px solid var(--sh3-border, #2a2a2a)}.muted.svelte-16gewpp{color:var(--sh3-muted, #888)}.danger.svelte-16gewpp{color:#e66}.r2-progress.svelte-putn2e{padding:8px;border:1px solid var(--sh3-border, #2a2a2a);border-radius:3px;margin:8px 0}.r2-progress__bar.svelte-putn2e{height:6px;background:var(--sh3-surface, #1a1a1a);margin:4px 0}.r2-progress__fill.svelte-putn2e{height:100%;background:var(--sh3-accent, #4a9eff)}.r2-progress__counts.svelte-putn2e{font-size:.85em;color:var(--sh3-muted, #888)}.r2-backup.svelte-8kfsmf{padding:12px;display:flex;flex-direction:column;gap:8px;height:100%;min-height:0;box-sizing:border-box}label.svelte-8kfsmf{display:flex;flex-direction:column;gap:2px;max-width:420px;flex:0 0 auto}.log-wrap.svelte-8kfsmf{flex:1 1 auto;min-height:0;overflow:auto;border:1px solid var(--sh3-border, #2a2a2a);border-radius:3px;padding:8px}select.svelte-8kfsmf{padding:var(--shell-pad-md) var(--shell-pad-lg);background:var(--shell-input-bg);color:var(--shell-fg);border:1px solid var(--shell-border);border-radius:var(--shell-radius);font-family:inherit;font-size:.8125rem;line-height:var(--shell-line)}select.svelte-8kfsmf:focus-visible{border-color:var(--shell-input-border-focus);box-shadow:var(--shell-focus-ring);outline:none}select.svelte-8kfsmf:disabled{opacity:.55;cursor:not-allowed}.r2-log.svelte-8kfsmf{list-style:none;padding:0;font-size:.85em}.r2-log.svelte-8kfsmf li:where(.svelte-8kfsmf){display:grid;grid-template-columns:auto 1fr auto auto;gap:8px;padding:2px 0}.r2-log.svelte-8kfsmf .status-failed:where(.svelte-8kfsmf){color:#e66}.r2-log.svelte-8kfsmf .status-skipped-unchanged:where(.svelte-8kfsmf){color:var(--sh3-muted, #888)}.muted.svelte-8kfsmf{color:var(--sh3-muted, #888)}.r2-tree.svelte-8cyv1p{width:100%;border-collapse:collapse;font-size:.9em}th.svelte-8cyv1p,td.svelte-8cyv1p{text-align:left;padding:4px 8px;border-bottom:1px solid var(--sh3-border, #2a2a2a)}tr.exists.svelte-8cyv1p{color:var(--sh3-muted, #888)}.badge.svelte-8cyv1p{margin-left:6px;background:var(--sh3-surface, #1a1a1a);padding:0 6px;border-radius:2px;font-size:.75em}.r2-import.svelte-1kdudbk{padding:12px;display:flex;flex-direction:column;gap:8px;height:100%;min-height:0;box-sizing:border-box}label.svelte-1kdudbk{display:flex;flex-direction:column;gap:2px;max-width:420px;flex:0 0 auto}.warn.svelte-1kdudbk{background:#5a3a1a;border:1px solid #a77733;padding:8px;color:#ffd;flex:0 0 auto}.summary.svelte-1kdudbk{border:1px solid var(--sh3-border, #2a2a2a);padding:8px;border-radius:3px;flex:0 0 auto}.actions.svelte-1kdudbk{display:flex;gap:8px;flex-wrap:wrap;flex:0 0 auto}.tree-wrap.svelte-1kdudbk{flex:1 1 auto;min-height:0;overflow:auto;border:1px solid var(--sh3-border, #2a2a2a);border-radius:3px}.r2-folder-dialog__backdrop.svelte-nj4o62{position:fixed;top:0;right:0;bottom:0;left:0;background:#00000073;display:flex;align-items:center;justify-content:center;z-index:9999}.r2-folder-dialog.svelte-nj4o62{background:var(--shell-bg, #1e1e1e);color:var(--shell-fg, #ddd);border:1px solid var(--shell-border, #444);border-radius:var(--shell-radius, 4px);padding:16px;min-width:360px;max-width:520px;display:flex;flex-direction:column;gap:10px}.r2-folder-dialog.svelte-nj4o62 header:where(.svelte-nj4o62) h2:where(.svelte-nj4o62){margin:0;font-size:1rem}.r2-folder-dialog__check.svelte-nj4o62{display:flex;align-items:center;gap:6px}.r2-folder-dialog__count.svelte-nj4o62{color:var(--shell-fg-muted, #888);margin:0}.r2-folder-dialog__errors.svelte-nj4o62{font-size:.85em;color:#e66;margin:4px 0 0;padding-left:18px}.r2-folder-dialog.svelte-nj4o62 footer:where(.svelte-nj4o62){display:flex;justify-content:flex-end;gap:6px;margin-top:6px}.r2-folder-dialog.svelte-nj4o62 button:where(.svelte-nj4o62){background:var(--shell-bg-elevated, #2a2a2a);color:inherit;border:1px solid var(--shell-border, #444);padding:4px 12px;font:inherit;cursor:pointer;border-radius:var(--shell-radius-sm, 3px)}.r2-folder-dialog.svelte-nj4o62 button.primary:where(.svelte-nj4o62){border-color:var(--shell-accent, #4a90e2)}.r2-folder-dialog.svelte-nj4o62 button[disabled]:where(.svelte-nj4o62){opacity:.5;cursor:not-allowed}.r2-folder-dialog.svelte-nj4o62 label:where(.svelte-nj4o62){display:flex;flex-direction:column;gap:2px}";
  document.head.appendChild(s);
})();
var we = Object.defineProperty;
var xe = (a, t, r) => t in a ? we(a, t, { enumerable: !0, configurable: !0, writable: !0, value: r }) : a[t] = r;
var ue = (a, t, r) => xe(a, typeof t != "symbol" ? t + "" : t, r);
import { shell as Q } from "sh3-core";
import { mount as le, unmount as ce } from "svelte";
import * as e from "svelte/internal/client";
import "svelte/internal/disclose-version";
const fe = "targets/";
function ke() {
  const a = new Uint8Array(8);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return `tgt-${t}`;
}
async function Ie(a) {
  const t = await a.list(), r = [];
  for (const o of t) {
    if (!o.path.startsWith(fe) || !o.path.endsWith(".json")) continue;
    const i = await a.read(o.path);
    if (i)
      try {
        r.push(JSON.parse(i));
      } catch {
      }
  }
  return r.sort((o, i) => o.label.localeCompare(i.label));
}
async function Se(a, t) {
  await a.write(`${fe}${t.id}.json`, JSON.stringify(t, null, 2));
}
async function Ce(a, t) {
  await a.delete(`${fe}${t}.json`);
}
const oe = "uploads/";
function Ae(a) {
  return a.slice(0, 7);
}
function Te() {
  const a = new Uint8Array(4);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return t;
}
async function Re(a, t) {
  const r = `${oe}${Ae(t.at)}/`, i = `${t.at.replace(/:/g, "-")}-${Te()}.json`;
  await a.write(`${r}${i}`, JSON.stringify(t, null, 2));
}
async function Be(a, t) {
  const r = await a.list(), o = /* @__PURE__ */ new Set();
  for (const s of r) {
    if (!s.path.startsWith(oe)) continue;
    const l = s.path.slice(oe.length), d = l.indexOf("/");
    d > 0 && o.add(l.slice(0, d));
  }
  const i = Array.from(o).sort().reverse().slice(0, 2), n = [];
  for (const s of r) {
    if (!i.find((h) => s.path.startsWith(`${oe}${h}/`)) || !s.path.endsWith(".json")) continue;
    const d = await a.read(s.path);
    if (d)
      try {
        n.push(JSON.parse(d));
      } catch {
      }
  }
  return n.sort((s, l) => s.at < l.at ? 1 : s.at > l.at ? -1 : 0), n.slice(0, t);
}
async function Pe(a) {
  const t = await a.list(), r = [];
  for (const o of t) {
    if (!o.path.startsWith(oe) || !o.path.endsWith(".json")) continue;
    const i = await a.read(o.path);
    if (i)
      try {
        const n = JSON.parse(i);
        n.status === "uploaded" && r.push(n);
      } catch {
      }
  }
  return r;
}
function je(a) {
  const t = /* @__PURE__ */ new Map();
  for (const r of a) {
    if (r.status !== "uploaded") continue;
    const o = `${r.shardId}/${r.path}`, i = t.get(o);
    (!i || r.at > i.at) && t.set(o, r);
  }
  return t;
}
function Ee() {
  const a = /* @__PURE__ */ new Set();
  return {
    subscribe(t) {
      return a.add(t), () => a.delete(t);
    },
    notify(t) {
      for (const r of a)
        try {
          r();
        } catch (o) {
          t == null || t(o);
        }
    }
  };
}
function Oe(a) {
  const t = a.documents({ format: "text" });
  let r = e.state(e.proxy([])), o = e.state(!1);
  const i = e.proxy({
    running: !1,
    currentLabel: "",
    total: 0,
    processed: 0,
    uploaded: 0,
    skipped: 0,
    failed: 0,
    errors: []
  });
  async function n() {
    e.set(r, await Ie(t), !0), e.set(o, !0);
  }
  let s = null, l = null;
  const d = Ee();
  function h() {
    d.notify((B) => console.error("[sh3-connector-r2] badge subscriber threw:", B));
  }
  async function m() {
    return s || l || (l = (async () => {
      const B = await Pe(t), k = je(B);
      return s = k, l = null, h(), k;
    })(), l);
  }
  function w() {
    return s;
  }
  function p(B) {
    if (B.status === "uploaded") {
      if (s) {
        const k = `${B.shardId}/${B.path}`, O = s.get(k);
        (!O || B.at > O.at) && s.set(k, B);
      }
      h();
    }
  }
  function T(B) {
    return d.subscribe(B);
  }
  return {
    ctx: a,
    docs: t,
    get targets() {
      return e.get(r);
    },
    get targetsLoaded() {
      return e.get(o);
    },
    progress: i,
    refreshTargets: n,
    peekBadgeIndex: w,
    ensureBadgeIndex: m,
    recordBadgeUpload: p,
    subscribeBadgeChange: T
  };
}
const Le = {
  md: "text/markdown",
  markdown: "text/markdown",
  json: "application/json",
  txt: "text/plain",
  guml: "text/plain",
  html: "text/html",
  htm: "text/html",
  css: "text/css",
  csv: "text/csv",
  xml: "application/xml",
  svg: "image/svg+xml",
  yaml: "application/yaml",
  yml: "application/yaml"
};
function De(a, t, r) {
  if (!t) throw new Error("buildObjectKey: shardId is required");
  if (r.split("/").some((n) => n === ".." || n === "."))
    throw new Error(`buildObjectKey: path traversal not allowed: ${r}`);
  const i = r.startsWith("/") ? r.slice(1) : r;
  return `${a}${t}/${i}`;
}
function Ke(a) {
  const t = a.lastIndexOf(".");
  if (t < 0) return "application/octet-stream";
  const r = a.slice(t + 1).toLowerCase();
  return Le[r] ?? "application/octet-stream";
}
const ve = "0123456789abcdef";
async function ge(a) {
  const t = typeof a == "string" ? new TextEncoder().encode(a) : a instanceof Uint8Array ? a : new Uint8Array(a), r = await crypto.subtle.digest("SHA-256", t), o = new Uint8Array(r);
  let i = "";
  for (let n = 0; n < o.length; n++)
    i += ve[o[n] >>> 4], i += ve[o[n] & 15];
  return i;
}
async function re(a, t) {
  const r = a instanceof Uint8Array ? a : new Uint8Array(a), o = await crypto.subtle.importKey(
    "raw",
    r,
    { name: "HMAC", hash: "SHA-256" },
    !1,
    ["sign"]
  ), i = await crypto.subtle.sign("HMAC", o, new TextEncoder().encode(t));
  return new Uint8Array(i);
}
function se() {
  const a = new Uint8Array(6);
  crypto.getRandomValues(a);
  let t = "";
  for (const r of a) t += r.toString(16).padStart(2, "0");
  return `${Date.now().toString(36)}-${t}`;
}
async function pe(a) {
  const { target: t, client: r, logHandle: o, readForeign: i, shardId: n, path: s } = a, l = De(t.keyPrefix, n, s), d = (/* @__PURE__ */ new Date()).toISOString();
  let h;
  try {
    h = await i(n, s);
  } catch (p) {
    const T = p instanceof Error ? p.message : String(p);
    return await ne(o, { id: se(), targetId: t.id, shardId: n, path: s, sha256: "", size: 0, status: "failed", reason: T, at: d }), { status: "failed", reason: T };
  }
  if (h === null) {
    const p = "Document not found at source";
    return await ne(o, { id: se(), targetId: t.id, shardId: n, path: s, sha256: "", size: 0, status: "failed", reason: p, at: d }), { status: "failed", reason: p };
  }
  const m = new TextEncoder().encode(h).length, w = await ge(h);
  try {
    const p = await r.headObject(l);
    if (p && p.sha256 === w)
      return await ne(o, { id: se(), targetId: t.id, shardId: n, path: s, sha256: w, size: m, status: "skipped-unchanged", at: d }), { status: "skipped-unchanged", sha256: w, size: m };
  } catch {
  }
  try {
    await r.putObject({
      key: l,
      body: h,
      contentType: Ke(s),
      sha256: w,
      metadata: {
        "sh3-shard": n,
        "sh3-path": encodeURIComponent(s),
        "sh3-uploaded-at": d,
        "sh3-source-tenant": a.sourceTenant ?? "",
        "sh3-sha256": w
      }
    });
    const p = { id: se(), targetId: t.id, shardId: n, path: s, sha256: w, size: m, status: "uploaded", at: d };
    return await ne(o, p), { status: "uploaded", sha256: w, size: m, entry: p };
  } catch (p) {
    const T = p instanceof Error ? p.message : String(p);
    return await ne(o, { id: se(), targetId: t.id, shardId: n, path: s, sha256: w, size: m, status: "failed", reason: T, at: d }), { status: "failed", reason: T };
  }
}
async function ne(a, t) {
  try {
    await Re(a, t);
  } catch (r) {
    console.warn("[sh3-connector-r2] failed to append upload log:", r);
  }
}
function He() {
  const a = /* @__PURE__ */ new Date(), t = (r) => String(r).padStart(2, "0");
  return `${a.getUTCFullYear()}${t(a.getUTCMonth() + 1)}${t(a.getUTCDate())}T${t(a.getUTCHours())}${t(a.getUTCMinutes())}${t(a.getUTCSeconds())}Z`;
}
function Me(a) {
  return a.slice(0, 8);
}
async function Ue(a) {
  const t = a.dateOverride ?? He(), r = Me(t), o = typeof a.payload == "string" ? new TextEncoder().encode(a.payload) : a.payload instanceof Uint8Array ? a.payload : new Uint8Array(a.payload), i = await ge(o), n = {};
  for (const [I, H] of Object.entries(a.headers ?? {}))
    n[I.toLowerCase()] = H;
  n.host = a.host, n["x-amz-date"] = t, a.service === "s3" && (n["x-amz-content-sha256"] = i);
  const s = Object.entries(n).map(([I, H]) => [I.toLowerCase(), String(H).trim().replace(/\s+/g, " ")]).sort(([I], [H]) => I < H ? -1 : I > H ? 1 : 0), l = s.map(([I]) => I).join(";"), d = s.map(([I, H]) => `${I}:${H}
`).join(""), h = [
    a.method.toUpperCase(),
    a.path,
    a.query ?? "",
    d,
    l,
    i
  ].join(`
`), m = `${r}/${a.region}/${a.service}/aws4_request`, w = [
    "AWS4-HMAC-SHA256",
    t,
    m,
    await ge(h)
  ].join(`
`), p = await re(
    new TextEncoder().encode(`AWS4${a.credentials.secretAccessKey}`),
    r
  ), T = await re(p, a.region), B = await re(T, a.service), k = await re(B, "aws4_request"), O = await re(k, w), K = Array.from(O).map((I) => I.toString(16).padStart(2, "0")).join(""), U = `AWS4-HMAC-SHA256 Credential=${a.credentials.accessKeyId}/${m}, SignedHeaders=${l}, Signature=${K}`, j = { ...n };
  return j.Authorization = U, { url: `https://${a.host}${a.path}${a.query ? `?${a.query}` : ""}`, method: a.method.toUpperCase(), headers: j, body: o };
}
class Ne extends Error {
  constructor(r, o, i, n) {
    super(r);
    ue(this, "name", "R2Error");
    this.status = o, this.code = i, this.responseText = n;
  }
}
async function Fe(a) {
  const t = fetch, r = a.body ?? "", o = await Ue({
    method: a.method,
    host: a.host,
    path: a.path,
    query: a.query ?? "",
    headers: a.headers,
    payload: r,
    service: a.service,
    region: a.region,
    credentials: a.credentials
  }), i = await t(o.url, {
    method: o.method,
    headers: o.headers,
    body: a.method === "GET" || a.method === "HEAD" ? void 0 : o.body
  });
  if (!i.ok) {
    const n = await i.text().catch(() => ""), s = n.match(/<Code>([^<]+)<\/Code>/);
    throw new Ne(
      `R2 ${a.method} ${a.path} failed (${i.status})`,
      i.status,
      s == null ? void 0 : s[1],
      n
    );
  }
  return i;
}
function te(a, t) {
  const r = `${a.accountId}.r2.cloudflarestorage.com`, o = {
    accessKeyId: a.accessKeyId,
    secretAccessKey: a.secretAccessKey
  }, i = `/${a.bucket}`;
  async function n(s, l, d = {}) {
    return Fe({
      method: s,
      host: r,
      path: `${i}${l}`,
      query: d.query,
      headers: d.headers,
      body: d.body,
      service: "s3",
      region: a.region,
      credentials: o
    });
  }
  return {
    async headBucket() {
      await n("HEAD", "/");
    },
    async putObject(s) {
      const l = {};
      for (const [h, m] of Object.entries(s.metadata ?? {}))
        l[`x-amz-meta-${h}`] = m;
      return { etag: (await n("PUT", `/${s.key}`, {
        body: s.body,
        headers: {
          "content-type": s.contentType,
          ...l
        }
      })).headers.get("etag") ?? "" };
    },
    async headObject(s) {
      try {
        const l = await n("HEAD", `/${s}`);
        return {
          size: Number(l.headers.get("content-length") ?? 0),
          sha256: l.headers.get("x-amz-meta-sh3-sha256") ?? void 0,
          etag: l.headers.get("etag") ?? "",
          lastModified: l.headers.get("last-modified") ?? void 0
        };
      } catch (l) {
        if (l instanceof Error && "status" in l && l.status === 404)
          return null;
        throw l;
      }
    },
    listObjectsV2({ prefix: s }) {
      return {
        async *[Symbol.asyncIterator]() {
          var d, h, m, w, p;
          let l = null;
          for (; ; ) {
            const T = new URLSearchParams({ "list-type": "2", prefix: s });
            l && T.set("continuation-token", l);
            const k = await (await n("GET", "/", { query: T.toString() })).text();
            for (const K of k.matchAll(/<Contents>([\s\S]*?)<\/Contents>/g)) {
              const U = K[1], j = ((d = /<Key>([^<]*)<\/Key>/.exec(U)) == null ? void 0 : d[1]) ?? "", R = Number(((h = /<Size>([^<]*)<\/Size>/.exec(U)) == null ? void 0 : h[1]) ?? "0"), I = ((m = /<LastModified>([^<]*)<\/LastModified>/.exec(U)) == null ? void 0 : m[1]) ?? "";
              yield { key: j, size: R, lastModified: I };
            }
            if (l = ((w = /<IsTruncated>([^<]*)<\/IsTruncated>/.exec(k)) == null ? void 0 : w[1]) === "true" ? ((p = /<NextContinuationToken>([^<]*)<\/NextContinuationToken>/.exec(k)) == null ? void 0 : p[1]) ?? null : null, !l) break;
          }
        }
      };
    },
    async getObject(s) {
      return (await n("GET", `/${s}`)).text();
    }
  };
}
class ie extends Error {
  constructor() {
    super(...arguments);
    ue(this, "name", "MissingCapabilityError");
  }
}
const qe = "documents:read not available. Reinstall the shard with the documents:read permission granted (sh3-core 0.9.1+).", ze = "documents:write not available. Reinstall the shard with the documents:write permission granted (sh3-core 0.9.1+).";
function de(a) {
  return async (t, r) => {
    var n;
    const o = (n = a.browse) == null ? void 0 : n.readFrom;
    if (!o) throw new ie(qe);
    const i = await o(t, r);
    if (i === null) return null;
    if (typeof i == "string") return i;
    throw new Error(
      `Binary document at ${t}/${r} is not supported in v0.1.0 (deferred).`
    );
  };
}
function We(a) {
  return async (t, r, o) => {
    var n;
    const i = (n = a.browse) == null ? void 0 : n.writeTo;
    if (!i) throw new ie(ze);
    await i(t, r, o);
  };
}
var Ge = e.from_html('<p class="r2-form__error svelte-h3b7wu"> </p>'), Ve = e.from_html('<button type="button" class="r2-form__hint svelte-h3b7wu">New to Cloudflare R2? Open the setup guide →</button>'), Je = e.from_html('<form class="r2-form svelte-h3b7wu"><label class="svelte-h3b7wu">Label<input type="text" required=""/></label> <label class="svelte-h3b7wu">Cloudflare Account ID<input type="text" required=""/></label> <label class="svelte-h3b7wu">Bucket name<input type="text" required=""/></label> <label class="svelte-h3b7wu">Key prefix (e.g. sh3-laptop/)<input type="text"/></label> <label class="svelte-h3b7wu">Access Key ID<input type="password" required=""/></label> <label class="svelte-h3b7wu">Secret Access Key<input type="password" required=""/></label> <!> <!> <div class="r2-form__actions svelte-h3b7wu"><button type="button">Cancel</button> <button type="submit"> </button></div></form>');
function me(a, t) {
  var g, _, b, A, M, N;
  e.push(t, !0);
  let r = e.state(e.proxy(((g = t.initial) == null ? void 0 : g.label) ?? "")), o = e.state(e.proxy(((_ = t.initial) == null ? void 0 : _.accountId) ?? "")), i = e.state(e.proxy(((b = t.initial) == null ? void 0 : b.bucket) ?? "")), n = e.state(e.proxy(((A = t.initial) == null ? void 0 : A.keyPrefix) ?? "")), s = e.state(e.proxy(((M = t.initial) == null ? void 0 : M.accessKeyId) ?? "")), l = e.state(e.proxy(((N = t.initial) == null ? void 0 : N.secretAccessKey) ?? "")), d = e.state(!1), h = e.state(null);
  async function m(x) {
    var V, P;
    x.preventDefault(), e.set(h, null), e.set(d, !0);
    const E = {
      id: ((V = t.initial) == null ? void 0 : V.id) ?? ke(),
      createdAt: ((P = t.initial) == null ? void 0 : P.createdAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
      label: e.get(r).trim(),
      accountId: e.get(o).trim(),
      bucket: e.get(i).trim(),
      region: "auto",
      keyPrefix: e.get(n).trim(),
      accessKeyId: e.get(s).trim(),
      secretAccessKey: e.get(l).trim()
    };
    try {
      await te(E).headBucket(), await t.onSave(E);
    } catch (y) {
      e.set(h, y instanceof Error ? y.message : String(y), !0);
    } finally {
      e.set(d, !1);
    }
  }
  var w = Je(), p = e.child(w), T = e.sibling(e.child(p));
  e.remove_input_defaults(T), e.reset(p);
  var B = e.sibling(p, 2), k = e.sibling(e.child(B));
  e.remove_input_defaults(k), e.reset(B);
  var O = e.sibling(B, 2), K = e.sibling(e.child(O));
  e.remove_input_defaults(K), e.reset(O);
  var U = e.sibling(O, 2), j = e.sibling(e.child(U));
  e.remove_input_defaults(j), e.reset(U);
  var R = e.sibling(U, 2), I = e.sibling(e.child(R));
  e.remove_input_defaults(I), e.reset(R);
  var H = e.sibling(R, 2), u = e.sibling(e.child(H));
  e.remove_input_defaults(u), e.reset(H);
  var S = e.sibling(H, 2);
  {
    var L = (x) => {
      var E = Ge(), V = e.child(E, !0);
      e.reset(E), e.template_effect(() => e.set_text(V, e.get(h))), e.append(x, E);
    };
    e.if(S, (x) => {
      e.get(h) && x(L);
    });
  }
  var z = e.sibling(S, 2);
  {
    var W = (x) => {
      var E = Ve();
      e.delegated("click", E, function(...V) {
        var P;
        (P = t.onShowGuide) == null || P.apply(this, V);
      }), e.append(x, E);
    };
    e.if(z, (x) => {
      t.onShowGuide && x(W);
    });
  }
  var G = e.sibling(z, 2), C = e.child(G), v = e.sibling(C, 2), c = e.child(v, !0);
  e.reset(v), e.reset(G), e.reset(w), e.template_effect(() => {
    C.disabled = e.get(d), v.disabled = e.get(d), e.set_text(c, e.get(d) ? "Validating…" : "Save & validate");
  }), e.event("submit", w, m), e.bind_value(T, () => e.get(r), (x) => e.set(r, x)), e.bind_value(k, () => e.get(o), (x) => e.set(o, x)), e.bind_value(K, () => e.get(i), (x) => e.set(i, x)), e.bind_value(j, () => e.get(n), (x) => e.set(n, x)), e.bind_value(I, () => e.get(s), (x) => e.set(s, x)), e.bind_value(u, () => e.get(l), (x) => e.set(l, x)), e.delegated("click", C, function(...x) {
    var E;
    (E = t.onCancel) == null || E.apply(this, x);
  }), e.append(a, w), e.pop();
}
e.delegate(["click"]);
var Ye = e.from_html(`<aside class="r2-guide svelte-122r6hr" aria-label="Cloudflare R2 setup guide"><header class="r2-guide__header svelte-122r6hr"><h3 class="svelte-122r6hr">Cloudflare R2 setup</h3> <button class="r2-guide__close svelte-122r6hr" type="button" aria-label="Close guide">×</button></header> <ol class="r2-guide__steps svelte-122r6hr"><li class="svelte-122r6hr"><strong>Sign in to Cloudflare.</strong> Go to <a href="https://dash.cloudflare.com" target="_blank" rel="noopener" class="svelte-122r6hr">dash.cloudflare.com</a>.
      R2 requires a payment method to enable, but the free tier covers 10 GB of storage and 1M Class A / 10M Class B ops per month.</li> <li class="svelte-122r6hr"><strong>Enable R2 and create a bucket.</strong> From the left sidebar, click <em>R2</em>. If R2 isn't enabled, click <em>Enable R2</em> and accept the terms.
      Then click <em>Create bucket</em>, give it a name (e.g. <code class="svelte-122r6hr">sh3-backups</code>), and pick a location hint close to you.</li> <li class="svelte-122r6hr"><strong>Copy your Account ID.</strong> On the R2 overview page, the right sidebar shows <em>Account ID</em>. Paste it into the form's Account ID field.</li> <li class="svelte-122r6hr"><strong>Create an API token.</strong> Top right of the R2 page: <em>Manage R2 API Tokens</em> → <em>Create API token</em>.
      Name it (e.g. <code class="svelte-122r6hr">sh3-connector-r2</code>), choose <em>Object Read &amp; Write</em>, scope it to your bucket.
      After creating, copy the <em>Access Key ID</em> and <em>Secret Access Key</em> — they are shown <strong>once</strong> — into the form.</li> <li class="svelte-122r6hr"><strong>Configure CORS on the bucket.</strong> Open your bucket → <em>Settings</em> → <em>CORS Policy</em> → <em>Edit CORS policy</em> → paste: <pre class="svelte-122r6hr"> </pre> Without this, the browser blocks uploads from this SH3 origin.</li> <li class="svelte-122r6hr"><strong>Fill in the form and save.</strong> Back in the left column: fill Label + Account ID + Bucket + Key prefix (optional) + Access Key + Secret. <em>Save &amp; validate</em> issues a signed <code class="svelte-122r6hr">HeadBucket</code> — a green result means credentials and CORS are both working.</li></ol></aside>`);
function Xe(a, t) {
  const o = `[
  {
    "AllowedOrigins": ["${typeof location < "u" ? location.origin : "<SH3 origin>"}"],
    "AllowedMethods": ["PUT", "GET", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"]
  }
]`;
  var i = Ye(), n = e.child(i), s = e.sibling(e.child(n), 2);
  e.reset(n);
  var l = e.sibling(n, 2), d = e.sibling(e.child(l), 8), h = e.sibling(e.child(d), 8), m = e.child(h, !0);
  e.reset(h), e.next(), e.reset(d), e.next(2), e.reset(l), e.reset(i), e.template_effect(() => e.set_text(m, o)), e.delegated("click", s, function(...w) {
    var p;
    (p = t.onClose) == null || p.apply(this, w);
  }), e.append(a, i);
}
e.delegate(["click"]);
var Ze = e.from_html('<button type="button">Help</button>'), Qe = e.from_html('<button type="button">Add target</button>'), $e = e.from_html("<p>Loading…</p>"), et = e.from_html(`<p class="muted svelte-16gewpp">No targets yet. Click "Add target" to configure one, or "Help" if you're new to Cloudflare R2.</p>`), tt = e.from_html('<tr><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"> </td><td class="svelte-16gewpp"><button type="button">Edit</button> <button type="button" class="danger svelte-16gewpp">Delete</button></td></tr>'), at = e.from_html('<table class="svelte-16gewpp"><thead><tr><th class="svelte-16gewpp">Label</th><th class="svelte-16gewpp">Bucket</th><th class="svelte-16gewpp">Prefix</th><th class="svelte-16gewpp"></th></tr></thead><tbody></tbody></table>'), rt = e.from_html('<div class="r2-targets__guide"><!></div>'), st = e.from_html('<div><div class="r2-targets__main"><header class="svelte-16gewpp"><h2>R2 Targets</h2> <div class="r2-targets__actions svelte-16gewpp"><!> <!></div></header> <!></div> <!></div>');
function nt(a, t) {
  e.push(t, !0);
  let r = e.state(null), o = e.state(!1);
  async function i(u) {
    await Se(t.rt.docs, u), await t.rt.refreshTargets(), e.set(r, null);
  }
  async function n(u) {
    confirm("Delete this target? Existing R2 objects are not removed.") && (await Ce(t.rt.docs, u), await t.rt.refreshTargets());
  }
  function s() {
    e.set(o, !0);
  }
  function l() {
    e.set(o, !1);
  }
  var d = st();
  let h;
  var m = e.child(d), w = e.child(m), p = e.sibling(e.child(w), 2), T = e.child(p);
  {
    var B = (u) => {
      var S = Ze();
      e.delegated("click", S, s), e.append(u, S);
    };
    e.if(T, (u) => {
      e.get(o) || u(B);
    });
  }
  var k = e.sibling(T, 2);
  {
    var O = (u) => {
      var S = Qe();
      e.delegated("click", S, () => e.set(r, "new")), e.append(u, S);
    };
    e.if(k, (u) => {
      e.get(r) === null && u(O);
    });
  }
  e.reset(p), e.reset(w);
  var K = e.sibling(w, 2);
  {
    var U = (u) => {
      me(u, {
        onSave: i,
        onCancel: () => e.set(r, null),
        onShowGuide: s
      });
    }, j = (u) => {
      me(u, {
        get initial() {
          return e.get(r);
        },
        onSave: i,
        onCancel: () => e.set(r, null),
        onShowGuide: s
      });
    }, R = (u) => {
      var S = e.comment(), L = e.first_child(S);
      {
        var z = (C) => {
          var v = $e();
          e.append(C, v);
        }, W = (C) => {
          var v = et();
          e.append(C, v);
        }, G = (C) => {
          var v = at(), c = e.sibling(e.child(v));
          e.each(c, 21, () => t.rt.targets, (g) => g.id, (g, _) => {
            var b = tt(), A = e.child(b), M = e.child(A, !0);
            e.reset(A);
            var N = e.sibling(A), x = e.child(N, !0);
            e.reset(N);
            var E = e.sibling(N), V = e.child(E, !0);
            e.reset(E);
            var P = e.sibling(E), y = e.child(P), f = e.sibling(y, 2);
            e.reset(P), e.reset(b), e.template_effect(() => {
              e.set_text(M, e.get(_).label), e.set_text(x, e.get(_).bucket), e.set_text(V, e.get(_).keyPrefix || "(none)");
            }), e.delegated("click", y, () => e.set(r, e.get(_), !0)), e.delegated("click", f, () => n(e.get(_).id)), e.append(g, b);
          }), e.reset(c), e.reset(v), e.append(C, v);
        };
        e.if(L, (C) => {
          t.rt.targetsLoaded ? t.rt.targets.length === 0 ? C(W, 1) : C(G, -1) : C(z);
        });
      }
      e.append(u, S);
    };
    e.if(K, (u) => {
      e.get(r) === "new" ? u(U) : e.get(r) && typeof e.get(r) != "string" ? u(j, 1) : u(R, -1);
    });
  }
  e.reset(m);
  var I = e.sibling(m, 2);
  {
    var H = (u) => {
      var S = rt(), L = e.child(S);
      Xe(L, { onClose: l }), e.reset(S), e.append(u, S);
    };
    e.if(I, (u) => {
      e.get(o) && u(H);
    });
  }
  e.reset(d), e.template_effect(() => h = e.set_class(d, 1, "r2-targets svelte-16gewpp", null, h, { "r2-targets--split": e.get(o) })), e.append(a, d), e.pop();
}
e.delegate(["click"]);
async function he(a) {
  const t = await a.list(), r = a.scope.pathPrefix ?? "", o = a.scope.recursive ?? !0, i = t.filter((s) => {
    if (s.shardId !== a.scope.shardId || !s.path.startsWith(r)) return !1;
    if (o) return !0;
    const l = s.path.slice(r.length);
    return l.length > 0 && !l.includes("/");
  }), n = { total: i.length, uploaded: 0, skipped: 0, failed: 0 };
  for (let s = 0; s < i.length; s++) {
    const l = await a.onItem(i[s], s, i.length);
    l === "uploaded" ? n.uploaded++ : l === "skipped-unchanged" ? n.skipped++ : n.failed++;
  }
  return n;
}
async function _e(a) {
  let t = 0, r = 0, o = 0, i = 0;
  const n = [];
  return { total: (await he({
    list: a.list,
    scope: { shardId: a.shardId, pathPrefix: a.pathPrefix, recursive: a.recursive },
    onItem: async (l, d, h) => {
      var w;
      const m = await a.upload(l);
      return i++, m.status === "uploaded" ? t++ : m.status === "skipped-unchanged" ? r++ : (o++, m.reason && n.push(`${l.path}: ${m.reason}`)), (w = a.onProgress) == null || w.call(a, {
        currentLabel: `${l.shardId}/${l.path}`,
        total: h,
        processed: i,
        uploaded: t,
        skipped: r,
        failed: o
      }), m.status;
    }
  })).total, uploaded: t, skipped: r, failed: o, errors: n };
}
var ot = e.from_html("<li> </li>"), it = e.from_html("<details><summary> </summary> <ul></ul></details>"), lt = e.from_html('<div class="r2-progress svelte-putn2e"><div class="r2-progress__label"><!></div> <div class="r2-progress__bar svelte-putn2e"><div class="r2-progress__fill svelte-putn2e"></div></div> <div class="r2-progress__counts svelte-putn2e"> </div> <!></div>');
function ct(a, t) {
  e.push(t, !0);
  const r = e.derived(() => t.p.total === 0 ? 0 : Math.round(t.p.processed / t.p.total * 100));
  var o = e.comment(), i = e.first_child(o);
  {
    var n = (s) => {
      var l = lt(), d = e.child(l), h = e.child(d);
      {
        var m = (j) => {
          var R = e.text();
          e.template_effect(() => e.set_text(R, `Running: ${t.p.currentLabel ?? ""}`)), e.append(j, R);
        }, w = (j) => {
          var R = e.text("Done.");
          e.append(j, R);
        };
        e.if(h, (j) => {
          t.p.running ? j(m) : j(w, -1);
        });
      }
      e.reset(d);
      var p = e.sibling(d, 2), T = e.child(p);
      let B;
      e.reset(p);
      var k = e.sibling(p, 2), O = e.child(k);
      e.reset(k);
      var K = e.sibling(k, 2);
      {
        var U = (j) => {
          var R = it(), I = e.child(R), H = e.child(I);
          e.reset(I);
          var u = e.sibling(I, 2);
          e.each(u, 21, () => t.p.errors, e.index, (S, L) => {
            var z = ot(), W = e.child(z, !0);
            e.reset(z), e.template_effect(() => e.set_text(W, e.get(L))), e.append(S, z);
          }), e.reset(u), e.reset(R), e.template_effect(() => e.set_text(H, `${t.p.errors.length ?? ""} error${t.p.errors.length === 1 ? "" : "s"}`)), e.append(j, R);
        };
        e.if(K, (j) => {
          t.p.errors.length > 0 && j(U);
        });
      }
      e.reset(l), e.template_effect(() => {
        B = e.set_style(T, "", B, { width: `${e.get(r) ?? ""}%` }), e.set_text(O, `${t.p.processed ?? ""}/${t.p.total ?? ""} — uploaded ${t.p.uploaded ?? ""}, skipped ${t.p.skipped ?? ""}, failed ${t.p.failed ?? ""}`);
      }), e.append(s, l);
    };
    e.if(i, (s) => {
      (t.p.running || t.p.total > 0) && s(n);
    });
  }
  e.append(a, o), e.pop();
}
var dt = e.from_html("<p>No R2 targets configured. Add one in the Targets tab.</p>"), ut = e.from_html("<option> </option>"), gt = e.from_html("<option> </option>"), ht = e.from_html('<p class="muted svelte-8kfsmf">No uploads yet.</p>'), ft = e.from_html('<span class="reason"> </span>'), pt = e.from_html('<li><span class="ts"> </span> <span class="path"> </span> <span class="status"> </span> <!></li>'), vt = e.from_html('<ul class="r2-log svelte-8kfsmf"></ul>'), mt = e.from_html('<label class="svelte-8kfsmf">Target <select class="svelte-8kfsmf"></select></label> <label class="svelte-8kfsmf">Shard <select class="svelte-8kfsmf"></select></label> <label class="svelte-8kfsmf">Path prefix (optional) <input type="text" placeholder="e.g. journal/"/></label> <button> </button> <!> <h3>Recent activity</h3> <div class="log-wrap svelte-8kfsmf"><!></div>', 1), bt = e.from_html('<div class="r2-backup svelte-8kfsmf"><h2>Back up to R2</h2> <!></div>');
function _t(a, t) {
  e.push(t, !0);
  let r = e.prop(t, "rt", 7), o = e.state(""), i = e.state(""), n = e.state(""), s = e.state(e.proxy([])), l = e.state(e.proxy([]));
  e.user_effect(() => {
    r().targets.length > 0 && !e.get(o) && e.set(o, r().targets[0].id, !0);
  });
  async function d() {
    r().ctx.browse && (e.set(s, await r().ctx.browse.listShards(), !0), !e.get(i) && e.get(s).length > 0 && e.set(i, e.get(s)[0], !0));
  }
  e.user_effect(() => {
    d();
  });
  async function h() {
    e.set(l, await Be(r().docs, 50), !0);
  }
  e.user_effect(() => {
    h();
  });
  async function m() {
    const k = r().targets.find((O) => O.id === e.get(o));
    if (!(!k || !r().ctx.browse)) {
      if (!e.get(i)) {
        Q.toast.notify("Pick a shard to back up.", { level: "warn" });
        return;
      }
      r().progress.running = !0, r().progress.currentLabel = "", r().progress.total = 0, r().progress.processed = 0, r().progress.uploaded = 0, r().progress.skipped = 0, r().progress.failed = 0, r().progress.errors = [];
      try {
        const O = te(k), K = de(r().ctx), U = r().ctx.browse, j = await _e({
          list: async () => (await U.listDocuments()).map((I) => ({ shardId: I.shardId, path: I.path })),
          shardId: e.get(i),
          pathPrefix: e.get(n) || void 0,
          recursive: !0,
          upload: async (R) => {
            const I = await pe({
              target: k,
              client: O,
              logHandle: r().docs,
              readForeign: K,
              shardId: R.shardId,
              path: R.path,
              sourceTenant: r().ctx.tenantId
            });
            return I.status === "uploaded" && I.entry && r().recordBadgeUpload(I.entry), I;
          },
          onProgress: (R) => {
            r().progress.currentLabel = R.currentLabel, r().progress.total = R.total, r().progress.processed = R.processed, r().progress.uploaded = R.uploaded, r().progress.skipped = R.skipped, r().progress.failed = R.failed;
          }
        });
        r().progress.errors = j.errors;
      } finally {
        r().progress.running = !1, await h();
      }
    }
  }
  var w = bt(), p = e.sibling(e.child(w), 2);
  {
    var T = (k) => {
      var O = dt();
      e.append(k, O);
    }, B = (k) => {
      var O = mt(), K = e.first_child(O), U = e.sibling(e.child(K));
      e.each(U, 21, () => r().targets, (v) => v.id, (v, c) => {
        var g = ut(), _ = e.child(g);
        e.reset(g);
        var b = {};
        e.template_effect(() => {
          e.set_text(_, `${e.get(c).label ?? ""} (${e.get(c).bucket ?? ""})`), b !== (b = e.get(c).id) && (g.value = (g.__value = e.get(c).id) ?? "");
        }), e.append(v, g);
      }), e.reset(U), e.reset(K);
      var j = e.sibling(K, 2), R = e.sibling(e.child(j));
      e.each(R, 21, () => e.get(s), e.index, (v, c) => {
        var g = gt(), _ = e.child(g, !0);
        e.reset(g);
        var b = {};
        e.template_effect(() => {
          e.set_text(_, e.get(c)), b !== (b = e.get(c)) && (g.value = (g.__value = e.get(c)) ?? "");
        }), e.append(v, g);
      }), e.reset(R), e.reset(j);
      var I = e.sibling(j, 2), H = e.sibling(e.child(I));
      e.remove_input_defaults(H), e.reset(I);
      var u = e.sibling(I, 2), S = e.child(u, !0);
      e.reset(u);
      var L = e.sibling(u, 2);
      ct(L, {
        get p() {
          return r().progress;
        }
      });
      var z = e.sibling(L, 4), W = e.child(z);
      {
        var G = (v) => {
          var c = ht();
          e.append(v, c);
        }, C = (v) => {
          var c = vt();
          e.each(c, 21, () => e.get(l), e.index, (g, _) => {
            var b = pt(), A = e.child(b), M = e.child(A, !0);
            e.reset(A);
            var N = e.sibling(A, 2), x = e.child(N);
            e.reset(N);
            var E = e.sibling(N, 2), V = e.child(E, !0);
            e.reset(E);
            var P = e.sibling(E, 2);
            {
              var y = (f) => {
                var F = ft(), D = e.child(F);
                e.reset(F), e.template_effect(() => e.set_text(D, `— ${e.get(_).reason ?? ""}`)), e.append(f, F);
              };
              e.if(P, (f) => {
                e.get(_).reason && f(y);
              });
            }
            e.reset(b), e.template_effect(() => {
              e.set_class(b, 1, `status-${e.get(_).status}`, "svelte-8kfsmf"), e.set_text(M, e.get(_).at), e.set_text(x, `${e.get(_).shardId ?? ""}/${e.get(_).path ?? ""}`), e.set_text(V, e.get(_).status);
            }), e.append(g, b);
          }), e.reset(c), e.append(v, c);
        };
        e.if(W, (v) => {
          e.get(l).length === 0 ? v(G) : v(C, -1);
        });
      }
      e.reset(z), e.template_effect(() => {
        u.disabled = r().progress.running, e.set_text(S, r().progress.running ? "Running…" : "Back up now");
      }), e.bind_select_value(U, () => e.get(o), (v) => e.set(o, v)), e.bind_select_value(R, () => e.get(i), (v) => e.set(i, v)), e.bind_value(H, () => e.get(n), (v) => e.set(n, v)), e.delegated("click", u, m), e.append(k, O);
    };
    e.if(p, (k) => {
      r().targets.length === 0 ? k(T) : k(B, -1);
    });
  }
  e.reset(w), e.append(a, w), e.pop();
}
e.delegate(["click"]);
function yt(a) {
  const t = a.lastIndexOf(".");
  if (t < 0) return;
  const r = a.lastIndexOf("/");
  if (!(t < r))
    return a.slice(t);
}
function wt(a) {
  const t = `${a.shardId}/${a.path}`, r = {
    origin: "local",
    version: a.localVersion,
    at: a.localAt,
    content: a.localContent
  }, o = {
    origin: `r2:${a.targetLabel}`,
    version: 0,
    at: a.incomingAt,
    content: a.incomingContent
  }, i = {
    id: t,
    label: t,
    branches: [r, o],
    meta: { remoteKey: a.remoteKey, shardId: a.shardId, path: a.path }
  }, n = yt(a.path);
  return n !== void 0 && (i.extension = n), i;
}
async function xt(a, t) {
  if (t.length === 0) return [];
  const r = t.map(wt), o = new Map(
    t.map((s) => [`${s.shardId}/${s.path}`, s])
  );
  let i;
  try {
    i = await a.resolve(r, { title: "Resolve R2 import conflicts" });
  } catch {
    return "cancelled";
  }
  if (i.status === "cancelled") return "cancelled";
  const n = [];
  for (const s of i.choices) {
    const l = o.get(s.itemId);
    if (!l) continue;
    const d = s.chosen.origin === "local" ? "local" : "incoming";
    n.push({
      shardId: l.shardId,
      path: l.path,
      choice: d,
      remoteKey: l.remoteKey
    });
  }
  for (const s of i.skipped) {
    const l = o.get(s);
    l && n.push({
      shardId: l.shardId,
      path: l.path,
      choice: "skipped",
      remoteKey: l.remoteKey
    });
  }
  return n;
}
var kt = e.from_html('<span class="badge svelte-8cyv1p" title="Already exists locally — unchecked by default to protect local copy">local</span>'), It = e.from_html('<tr><td class="svelte-8cyv1p"><input type="checkbox" class="shell-base-check"/></td><td class="svelte-8cyv1p"> <!></td><td class="svelte-8cyv1p"> </td><td class="svelte-8cyv1p"> </td></tr>'), St = e.from_html('<table class="r2-tree svelte-8cyv1p"><thead><tr><th class="svelte-8cyv1p"></th><th class="svelte-8cyv1p">Key</th><th class="svelte-8cyv1p">Size</th><th class="svelte-8cyv1p">Modified</th></tr></thead><tbody></tbody></table>');
function Ct(a, t) {
  e.push(t, !0);
  var r = St(), o = e.sibling(e.child(r));
  e.each(o, 21, () => t.nodes, (i) => i.path, (i, n) => {
    var s = It(), l = e.child(s), d = e.child(l);
    e.remove_input_defaults(d), e.reset(l);
    var h = e.sibling(l), m = e.child(h), w = e.sibling(m);
    {
      var p = (K) => {
        var U = kt();
        e.append(K, U);
      };
      e.if(w, (K) => {
        e.get(n).existsLocal && K(p);
      });
    }
    e.reset(h);
    var T = e.sibling(h), B = e.child(T, !0);
    e.reset(T);
    var k = e.sibling(T), O = e.child(k, !0);
    e.reset(k), e.reset(s), e.template_effect(
      (K) => {
        e.set_class(s, 1, e.clsx(e.get(n).existsLocal ? "exists" : ""), "svelte-8cyv1p"), e.set_checked(d, K), e.set_text(m, `${e.get(n).path ?? ""} `), e.set_text(B, e.get(n).size), e.set_text(O, e.get(n).lastModified);
      },
      [() => t.selected.has(e.get(n).path)]
    ), e.delegated("change", d, () => t.onToggle(e.get(n).path)), e.append(i, s);
  }), e.reset(o), e.reset(r), e.append(a, r), e.pop();
}
e.delegate(["change"]);
var At = e.from_html(`<div class="warn svelte-1kdudbk">Import requires the <code>documents:write</code> permission (sh3-core 0.9.1+).
      Reinstall the shard and grant this permission when prompted.</div>`), Tt = e.from_html("<p>No R2 targets configured. Add one in the Targets tab.</p>"), Rt = e.from_html("<option> </option>"), Bt = e.from_html('<p> </p> <div class="tree-wrap svelte-1kdudbk"><!></div> <div class="actions svelte-1kdudbk"><button> </button> <button> </button> <button> </button></div>', 1), Pt = e.from_html("<li> </li>"), jt = e.from_html("<details><summary>Errors</summary><ul></ul></details>"), Et = e.from_html('<div class="summary svelte-1kdudbk"> <!></div>'), Ot = e.from_html('<label class="svelte-1kdudbk">Target <select></select></label> <button> </button> <!> <!>', 1), Lt = e.from_html('<div class="r2-import svelte-1kdudbk"><h2>Import from R2</h2> <!> <!></div>');
function Dt(a, t) {
  var C, v;
  e.push(t, !0);
  let r = e.state("");
  function o() {
    return {
      imported: 0,
      resolvedLocal: 0,
      resolvedIncoming: 0,
      skipped: 0,
      cancelled: 0,
      failed: 0,
      errors: []
    };
  }
  let i = e.state(e.proxy([])), n = e.state(e.proxy(/* @__PURE__ */ new Set())), s = e.state(!1), l = e.state(!1), d = e.state(!1), h = e.state(null);
  const m = typeof ((v = (C = Q) == null ? void 0 : C.conflicts) == null ? void 0 : v.resolve) == "function", w = e.derived(() => e.get(i).filter((c) => e.get(n).has(c.path))), p = e.derived(() => e.get(w).filter((c) => !c.existsLocal)), T = e.derived(() => e.get(w).filter((c) => c.existsLocal && !c.binaryUnsupported)), B = e.derived(() => e.get(w).filter((c) => c.existsLocal && c.binaryUnsupported)), k = e.derived(() => e.get(p).length > 0 && e.get(T).length === 0 && e.get(B).length === 0), O = e.derived(() => m && e.get(T).length > 0 && e.get(p).length === 0), K = e.derived(() => m && e.get(p).length > 0 && e.get(T).length > 0);
  e.user_effect(() => {
    t.rt.targets.length > 0 && !e.get(r) && e.set(r, t.rt.targets[0].id, !0);
  });
  async function U() {
    const c = t.rt.targets.find((g) => g.id === e.get(r));
    if (!(!c || !t.rt.ctx.browse)) {
      e.set(h, null), e.set(s, !0);
      try {
        const g = te(c), _ = de(t.rt.ctx), b = t.rt.ctx.browse.statusFrom, A = /* @__PURE__ */ new Set();
        for (const x of await t.rt.ctx.browse.listDocuments()) A.add(`${x.shardId}/${x.path}`);
        const M = [], N = /* @__PURE__ */ new Set();
        for await (const x of g.listObjectsV2({ prefix: c.keyPrefix })) {
          const E = x.key.slice(c.keyPrefix.length), V = E.indexOf("/");
          if (V < 0) continue;
          const P = E.slice(0, V), y = E.slice(V + 1), f = A.has(`${P}/${y}`);
          let F, D, Y = !1;
          if (f) {
            try {
              F = await _(P, y) ?? "";
            } catch (J) {
              if (J instanceof ie) {
                e.set(d, !0), e.set(s, !1);
                return;
              }
              Y = !0;
            }
            if (b)
              try {
                const J = await b(P, y);
                J && (D = J);
              } catch {
              }
          }
          M.push({
            path: x.key,
            size: x.size,
            lastModified: x.lastModified,
            existsLocal: f,
            shardId: P,
            docPath: y,
            localContent: F,
            localStatus: D,
            binaryUnsupported: Y || void 0
          }), f || N.add(x.key);
        }
        e.set(i, M, !0), e.set(n, N, !0);
      } finally {
        e.set(s, !1);
      }
    }
  }
  function j(c) {
    e.get(n).has(c) ? e.get(n).delete(c) : e.get(n).add(c), e.set(n, new Set(e.get(n)), !0);
  }
  async function R(c, g, _, b) {
    for (const A of b)
      try {
        const M = await c.getObject(A.path);
        await g(A.shardId, A.docPath, M), _.imported++;
      } catch (M) {
        if (M instanceof ie)
          return e.set(d, !0), !1;
        _.failed++, _.errors.push(`${A.path}: ${M instanceof Error ? M.message : String(M)}`);
      }
    return !0;
  }
  async function I(c, g, _, b, A, M) {
    var V, P, y;
    for (const f of M)
      b.failed++, b.errors.push(`${f.path}: binary not supported`);
    if (A.length === 0 || !((V = Q) != null && V.conflicts)) return !0;
    const N = [];
    for (const f of A)
      try {
        const F = await g.getObject(f.path), D = Date.parse(f.lastModified);
        N.push({
          shardId: f.shardId,
          path: f.docPath,
          localContent: f.localContent ?? "",
          localVersion: ((P = f.localStatus) == null ? void 0 : P.version) ?? 1,
          localAt: ((y = f.localStatus) == null ? void 0 : y.lastSyncedAt) ?? 0,
          incomingContent: F,
          incomingAt: Number.isFinite(D) ? D : Date.now(),
          targetLabel: c.label,
          remoteKey: f.path
        });
      } catch (F) {
        b.failed++, b.errors.push(`${f.path}: ${F instanceof Error ? F.message : String(F)}`);
      }
    if (N.length === 0) return !0;
    const x = await xt(Q.conflicts, N);
    if (x === "cancelled")
      return b.cancelled += N.length, !0;
    const E = new Map(N.map((f) => [f.remoteKey, f]));
    for (const f of x)
      if (f.choice === "local")
        b.resolvedLocal++;
      else if (f.choice === "skipped")
        b.skipped++;
      else {
        const F = E.get(f.remoteKey);
        if (!F) continue;
        try {
          await _(f.shardId, f.path, F.incomingContent), b.resolvedIncoming++;
        } catch (D) {
          if (D instanceof ie)
            return e.set(d, !0), !1;
          b.failed++, b.errors.push(`${f.shardId}/${f.path}: ${D instanceof Error ? D.message : String(D)}`);
        }
      }
    return !0;
  }
  async function H(c) {
    const g = t.rt.targets.find((b) => b.id === e.get(r));
    if (!g) return;
    e.set(h, null), e.set(l, !0);
    const _ = o();
    try {
      const b = te(g), A = We(t.rt.ctx);
      if ((c === "new" || c === "mixed") && !await R(b, A, _, e.get(p)))
        return;
      (c === "updated" || c === "mixed") && await I(g, b, A, _, e.get(T), c === "updated" ? e.get(B) : []);
    } finally {
      e.set(l, !1), e.set(h, _, !0);
    }
  }
  var u = Lt(), S = e.sibling(e.child(u), 2);
  {
    var L = (c) => {
      var g = At();
      e.append(c, g);
    };
    e.if(S, (c) => {
      e.get(d) && c(L);
    });
  }
  var z = e.sibling(S, 2);
  {
    var W = (c) => {
      var g = Tt();
      e.append(c, g);
    }, G = (c) => {
      var g = Ot(), _ = e.first_child(g), b = e.sibling(e.child(_));
      e.each(b, 21, () => t.rt.targets, (P) => P.id, (P, y) => {
        var f = Rt(), F = e.child(f);
        e.reset(f);
        var D = {};
        e.template_effect(() => {
          e.set_text(F, `${e.get(y).label ?? ""} (${e.get(y).bucket ?? ""})`), D !== (D = e.get(y).id) && (f.value = (f.__value = e.get(y).id) ?? "");
        }), e.append(P, f);
      }), e.reset(b), e.reset(_);
      var A = e.sibling(_, 2), M = e.child(A, !0);
      e.reset(A);
      var N = e.sibling(A, 2);
      {
        var x = (P) => {
          var y = Bt(), f = e.first_child(y), F = e.child(f);
          e.reset(f);
          var D = e.sibling(f, 2), Y = e.child(D);
          Ct(Y, {
            get nodes() {
              return e.get(i);
            },
            get selected() {
              return e.get(n);
            },
            onToggle: j
          }), e.reset(D);
          var J = e.sibling(D, 2), X = e.child(J), ee = e.child(X, !0);
          e.reset(X);
          var Z = e.sibling(X, 2), ae = e.child(Z, !0);
          e.reset(Z);
          var $ = e.sibling(Z, 2), ye = e.child($, !0);
          e.reset($), e.reset(J), e.template_effect(() => {
            e.set_text(F, `${e.get(i).length ?? ""} object${e.get(i).length === 1 ? "" : "s"} — ${e.get(n).size ?? ""} selected`), X.disabled = e.get(l) || !e.get(k), e.set_text(ee, e.get(l) ? "Importing…" : `Import New (${e.get(p).length})`), Z.disabled = e.get(l) || !e.get(O), e.set_attribute(Z, "title", m ? "" : "Requires sh3-core 0.10.4+"), e.set_text(ae, e.get(l) ? "Importing…" : `Import Updated (${e.get(T).length})`), $.disabled = e.get(l) || !e.get(K), e.set_attribute($, "title", m ? "" : "Requires sh3-core 0.10.4+"), e.set_text(ye, e.get(l) ? "Importing…" : "Import Selected");
          }), e.delegated("click", X, () => H("new")), e.delegated("click", Z, () => H("updated")), e.delegated("click", $, () => H("mixed")), e.append(P, y);
        };
        e.if(N, (P) => {
          e.get(i).length > 0 && P(x);
        });
      }
      var E = e.sibling(N, 2);
      {
        var V = (P) => {
          var y = Et(), f = e.child(y), F = e.sibling(f);
          {
            var D = (Y) => {
              var J = jt(), X = e.sibling(e.child(J));
              e.each(X, 21, () => e.get(h).errors, e.index, (ee, Z) => {
                var ae = Pt(), $ = e.child(ae, !0);
                e.reset(ae), e.template_effect(() => e.set_text($, e.get(Z))), e.append(ee, ae);
              }), e.reset(X), e.reset(J), e.append(Y, J);
            };
            e.if(F, (Y) => {
              e.get(h).errors.length > 0 && Y(D);
            });
          }
          e.reset(y), e.template_effect(() => e.set_text(f, `Imported: ${e.get(h).imported ?? ""}
        · Kept local: ${e.get(h).resolvedLocal ?? ""}
        · Applied remote: ${e.get(h).resolvedIncoming ?? ""}
        · Skipped: ${e.get(h).skipped ?? ""}
        · Cancelled: ${e.get(h).cancelled ?? ""}
        · Failed: ${e.get(h).failed ?? ""} `)), e.append(P, y);
        };
        e.if(E, (P) => {
          e.get(h) && P(V);
        });
      }
      e.template_effect(() => {
        A.disabled = e.get(s), e.set_text(M, e.get(s) ? "Scanning…" : "Scan bucket");
      }), e.bind_select_value(b, () => e.get(r), (P) => e.set(r, P)), e.delegated("click", A, U), e.append(c, g);
    };
    e.if(z, (c) => {
      t.rt.targets.length === 0 ? c(W) : c(G, -1);
    });
  }
  e.reset(u), e.append(a, u), e.pop();
}
e.delegate(["click"]);
var Kt = e.from_html("<p>No R2 targets configured. Open the R2 Backup app to add one.</p>"), Ht = e.from_html("<option> </option>"), Mt = e.from_html('<label class="svelte-nj4o62">Target <select></select></label>'), Ut = e.from_html('<!> <label class="r2-folder-dialog__check svelte-nj4o62"><input type="checkbox"/> Include subfolders</label> <p class="r2-folder-dialog__count svelte-nj4o62"><!></p>', 1), Nt = e.from_html('<!> <footer class="svelte-nj4o62"><button class="svelte-nj4o62">Cancel</button> <button class="primary svelte-nj4o62">Back up</button></footer>', 1), Ft = e.from_html("<p> </p> <p> </p>", 1), qt = e.from_html("<li> </li>"), zt = e.from_html('<ul class="r2-folder-dialog__errors svelte-nj4o62"></ul>'), Wt = e.from_html('<p> </p> <!> <footer class="svelte-nj4o62"><button class="primary svelte-nj4o62">Close</button></footer>', 1), Gt = e.from_html('<div class="r2-folder-dialog__backdrop svelte-nj4o62" role="presentation"><div class="r2-folder-dialog svelte-nj4o62" role="dialog" aria-modal="true"><header class="svelte-nj4o62"><h2 class="svelte-nj4o62"> </h2></header> <!></div></div>');
function Vt(a, t) {
  var H;
  e.push(t, !0);
  let r = e.state(!0), o = e.state(null), i = e.state(null), n = e.state(e.proxy(((H = t.rt.targets[0]) == null ? void 0 : H.id) ?? "")), s = e.state("preview"), l = e.state(null), d = e.state(null);
  const h = e.derived(() => t.pathPrefix ? t.pathPrefix.replace(/\/$/, "").split("/").pop() ?? t.pathPrefix : "(shard root)"), m = e.derived(() => e.get(r) ? e.get(o) : e.get(i));
  async function w() {
    if (!t.rt.ctx.browse) return;
    const u = t.rt.ctx.browse, S = async () => (await u.listDocuments()).map((C) => ({ shardId: C.shardId, path: C.path })), L = t.pathPrefix.endsWith("/") || t.pathPrefix === "" ? t.pathPrefix : `${t.pathPrefix}/`, z = await he({
      list: S,
      scope: {
        shardId: t.shardId,
        pathPrefix: L,
        recursive: !0
      },
      onItem: async () => "skipped-unchanged"
    }), W = await he({
      list: S,
      scope: {
        shardId: t.shardId,
        pathPrefix: L,
        recursive: !1
      },
      onItem: async () => "skipped-unchanged"
    });
    e.set(o, z.total, !0), e.set(i, W.total, !0);
  }
  e.user_effect(() => {
    w();
  });
  async function p() {
    const u = t.rt.targets.find((C) => C.id === e.get(n));
    if (!u || !t.rt.ctx.browse) return;
    e.set(s, "running"), e.set(l, null);
    const S = t.rt.ctx.browse, L = te(u), z = de(t.rt.ctx), W = t.pathPrefix.endsWith("/") || t.pathPrefix === "" ? t.pathPrefix : `${t.pathPrefix}/`, G = await _e({
      list: async () => (await S.listDocuments()).map((v) => ({ shardId: v.shardId, path: v.path })),
      shardId: t.shardId,
      pathPrefix: W || void 0,
      recursive: e.get(r),
      upload: async (C) => {
        const v = await pe({
          target: u,
          client: L,
          logHandle: t.rt.docs,
          readForeign: z,
          shardId: C.shardId,
          path: C.path,
          sourceTenant: t.rt.ctx.tenantId
        });
        return v.status === "uploaded" && v.entry && t.rt.recordBadgeUpload(v.entry), v;
      },
      onProgress: (C) => {
        e.set(l, C, !0);
      }
    });
    e.set(
      d,
      {
        uploaded: G.uploaded,
        skipped: G.skipped,
        failed: G.failed,
        errors: G.errors
      },
      !0
    ), e.set(s, "done");
  }
  var T = Gt(), B = e.child(T), k = e.child(B), O = e.child(k), K = e.child(O);
  e.reset(O), e.reset(k);
  var U = e.sibling(k, 2);
  {
    var j = (u) => {
      var S = Nt(), L = e.first_child(S);
      {
        var z = (c) => {
          var g = Kt();
          e.append(c, g);
        }, W = (c) => {
          var g = Ut(), _ = e.first_child(g);
          {
            var b = (y) => {
              var f = Mt(), F = e.sibling(e.child(f));
              e.each(F, 21, () => t.rt.targets, (D) => D.id, (D, Y) => {
                var J = Ht(), X = e.child(J);
                e.reset(J);
                var ee = {};
                e.template_effect(() => {
                  e.set_text(X, `${e.get(Y).label ?? ""} (${e.get(Y).bucket ?? ""})`), ee !== (ee = e.get(Y).id) && (J.value = (J.__value = e.get(Y).id) ?? "");
                }), e.append(D, J);
              }), e.reset(F), e.reset(f), e.bind_select_value(F, () => e.get(n), (D) => e.set(n, D)), e.append(y, f);
            };
            e.if(_, (y) => {
              t.rt.targets.length > 1 && y(b);
            });
          }
          var A = e.sibling(_, 2), M = e.child(A);
          e.remove_input_defaults(M), e.next(), e.reset(A);
          var N = e.sibling(A, 2), x = e.child(N);
          {
            var E = (y) => {
              var f = e.text("Counting…");
              e.append(y, f);
            }, V = (y) => {
              var f = e.text("0 documents will be uploaded.");
              e.append(y, f);
            }, P = (y) => {
              var f = e.text();
              e.template_effect(() => e.set_text(f, `${e.get(m) ?? ""} document${e.get(m) === 1 ? "" : "s"} will be uploaded.`)), e.append(y, f);
            };
            e.if(x, (y) => {
              e.get(m) === null ? y(E) : e.get(m) === 0 ? y(V, 1) : y(P, -1);
            });
          }
          e.reset(N), e.bind_checked(M, () => e.get(r), (y) => e.set(r, y)), e.append(c, g);
        };
        e.if(L, (c) => {
          t.rt.targets.length === 0 ? c(z) : c(W, -1);
        });
      }
      var G = e.sibling(L, 2), C = e.child(G), v = e.sibling(C, 2);
      e.reset(G), e.template_effect(() => v.disabled = t.rt.targets.length === 0 || e.get(m) === 0 || e.get(m) === null || !e.get(n)), e.delegated("click", C, function(...c) {
        var g;
        (g = t.onClose) == null || g.apply(this, c);
      }), e.delegated("click", v, p), e.append(u, S);
    }, R = (u) => {
      var S = Ft(), L = e.first_child(S), z = e.child(L, !0);
      e.reset(L);
      var W = e.sibling(L, 2), G = e.child(W);
      e.reset(W), e.template_effect(() => {
        var C, v, c;
        e.set_text(z, ((C = e.get(l)) == null ? void 0 : C.currentLabel) ?? "Starting…"), e.set_text(G, `${((v = e.get(l)) == null ? void 0 : v.processed) ?? 0 ?? ""} / ${((c = e.get(l)) == null ? void 0 : c.total) ?? 0 ?? ""}`);
      }), e.append(u, S);
    }, I = (u) => {
      var S = Wt(), L = e.first_child(S), z = e.child(L);
      e.reset(L);
      var W = e.sibling(L, 2);
      {
        var G = (c) => {
          var g = zt();
          e.each(g, 21, () => e.get(d).errors, e.index, (_, b) => {
            var A = qt(), M = e.child(A, !0);
            e.reset(A), e.template_effect(() => e.set_text(M, e.get(b))), e.append(_, A);
          }), e.reset(g), e.append(c, g);
        };
        e.if(W, (c) => {
          e.get(d) && e.get(d).errors.length > 0 && c(G);
        });
      }
      var C = e.sibling(W, 2), v = e.child(C);
      e.reset(C), e.template_effect(() => {
        var c, g, _;
        return e.set_text(z, `${((c = e.get(d)) == null ? void 0 : c.uploaded) ?? ""} uploaded, ${((g = e.get(d)) == null ? void 0 : g.skipped) ?? ""} skipped, ${((_ = e.get(d)) == null ? void 0 : _.failed) ?? ""} failed.`);
      }), e.delegated("click", v, function(...c) {
        var g;
        (g = t.onClose) == null || g.apply(this, c);
      }), e.append(u, S);
    };
    e.if(U, (u) => {
      e.get(s) === "preview" ? u(j) : e.get(s) === "running" ? u(R, 1) : u(I, -1);
    });
  }
  e.reset(B), e.reset(T), e.template_effect(() => e.set_text(K, `Back up "${e.get(h) ?? ""}" to R2`)), e.delegated("click", T, function(...u) {
    var S;
    (S = t.onClose) == null || S.apply(this, u);
  }), e.delegated("click", B, (u) => u.stopPropagation()), e.append(a, T), e.pop();
}
e.delegate(["click"]);
function Jt(a) {
  const t = document.createElement("div");
  t.className = "r2-folder-dialog-host", document.body.appendChild(t);
  let r = null;
  const o = () => {
    r && ce(r), r = null, t.remove();
  };
  r = le(Vt, {
    target: t,
    props: { rt: a.rt, shardId: a.shardId, pathPrefix: a.pathPrefix, onClose: o }
  });
}
function Yt(a, t) {
  return t.kind === "folder" ? Zt(a, t) : Xt(a, t);
}
function Xt(a, t) {
  const r = a.get(`${t.shardId}/${t.path}`);
  return r ? t.lastModified !== void 0 && t.lastModified > Date.parse(r.at) ? {
    icon: "⚠",
    tone: "warn",
    tooltip: "Edited after last backup",
    label: "Backup is stale",
    detail: `Last backup ${r.at}`
  } : {
    icon: "☁",
    tone: "ok",
    tooltip: "Backed up",
    label: "Backed up to R2",
    detail: r.at
  } : null;
}
function Zt(a, t) {
  if (t.descendantCount === 0) return null;
  const r = `${t.shardId}/${t.path}${t.path ? "/" : ""}`;
  let o = 0;
  for (const l of a.keys()) l.startsWith(r) && o++;
  if (o === 0) return null;
  const i = t.descendantCount, n = i !== void 0 ? Math.min(o, i) : o, s = i !== void 0 ? `${n}/${i} backed up` : `${n} backed up`;
  return { icon: "☁", tone: "ok", tooltip: s, label: s };
}
const be = "sh3-file-explorer.selectionAction", Qt = "sh3-file-explorer.documentBadge";
let q = null;
const ra = {
  manifest: {
    id: "sh3-connector-r2",
    label: "R2 Backup",
    views: [
      { id: "sh3-connector-r2-targets", label: "Targets" },
      { id: "sh3-connector-r2-backup", label: "Backup" },
      { id: "sh3-connector-r2-import", label: "Import" }
    ],
    permissions: ["documents:browse", "documents:read", "documents:write"]
  },
  // Self-starting marker: keeps the shard active across the whole tenant
  // session so the SelectionAction contributions stay in the registry while
  // file-explorer (or any other consuming app) is open without R2 Backup.
  autostart() {
  },
  async activate(a) {
    q = Oe(a), q.refreshTargets(), q.ensureBadgeIndex(), a.registerView("sh3-connector-r2-targets", {
      mount(t) {
        const r = le(nt, { target: t, props: { rt: q } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.registerView("sh3-connector-r2-backup", {
      mount(t) {
        const r = le(_t, { target: t, props: { rt: q } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.registerView("sh3-connector-r2-import", {
      mount(t) {
        const r = le(Dt, { target: t, props: { rt: q } });
        return { unmount() {
          ce(r);
        } };
      }
    }), a.contributions.register(be, {
      id: "sh3-connector-r2:backup-file",
      label: "Back up to R2",
      appliesTo: (t) => t.kind === "file" && ((q == null ? void 0 : q.targets.length) ?? 0) > 0,
      onInvoke: async (t) => {
        if (!q) return;
        const r = q.targets;
        if (r.length === 0) {
          Q.toast.notify("No R2 targets configured. Open the R2 Backup app to add one.", { level: "warn" });
          return;
        }
        const o = r.length === 1 ? r[0] : r.find((d) => d.label === window.prompt(
          `Target label (${r.map((h) => h.label).join(", ")}):`,
          r[0].label
        ));
        if (!o) return;
        const i = te(o), n = de(a), s = await pe({
          target: o,
          client: i,
          logHandle: q.docs,
          readForeign: n,
          shardId: t.shardId,
          path: t.path,
          sourceTenant: a.tenantId
        });
        s.status === "uploaded" && s.entry && q.recordBadgeUpload(s.entry);
        const l = `${t.shardId}/${t.path}`;
        s.status === "uploaded" ? Q.toast.notify(`Backed up ${l}`, { level: "success" }) : s.status === "skipped-unchanged" ? Q.toast.notify(`Already up to date — ${l}`, { level: "info" }) : Q.toast.notify(`Backup failed: ${s.reason ?? "unknown"} — ${l}`, { level: "error" });
      },
      kind: "secondary"
    }), a.contributions.register(be, {
      id: "sh3-connector-r2:backup-folder",
      label: "Back up to R2…",
      appliesTo: (t) => t.kind === "folder" && ((q == null ? void 0 : q.targets.length) ?? 0) > 0,
      onInvoke: (t) => {
        q && Jt({
          rt: q,
          shardId: t.shardId,
          pathPrefix: t.path
        });
      },
      kind: "secondary"
    }), a.contributions.register(Qt, {
      id: "sh3-connector-r2:backup-status",
      getBadge: (t) => {
        if (!q) return null;
        const r = q.peekBadgeIndex();
        return r ? Yt(r, t) : null;
      },
      onChange: (t) => (q == null ? void 0 : q.subscribeBadgeChange(t)) ?? (() => {
      })
    });
  },
  deactivate() {
    q = null;
  }
}, sa = {
  manifest: {
    id: "sh3-connector-r2-app",
    label: "R2 Backup",
    requiredShards: ["sh3-connector-r2"],
    layoutVersion: 2,
    permissions: ["documents:browse"]
  },
  initialLayout: {
    type: "tabs",
    activeTab: 0,
    tabs: [
      { slotId: "sh3-connector-r2.targets", viewId: "sh3-connector-r2-targets", label: "Targets" },
      { slotId: "sh3-connector-r2.backup", viewId: "sh3-connector-r2-backup", label: "Backup" },
      { slotId: "sh3-connector-r2.import", viewId: "sh3-connector-r2-import", label: "Import" }
    ]
  }
};
export {
  sa as app,
  ra as shard
};
