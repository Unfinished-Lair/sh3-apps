{
  "name": "sh3-deepseek-shell",
  "version": "0.2.4",
  "description": "DeepSeek integration for the SH3 shell — registers as an sh3-ai.provider; appless, settings reachable via command palette",
  "author": "@unfinished-lair",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "vite build",
    "build:artifact": "vite build --config vite.artifact.ts",
    "dev": "vite build --watch",
    "test": "vitest run"
  },
  "peerDependencies": {
    "sh3-ai": "^0.4.5",
    "sh3-core": "^0.15.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "sh3-ai": "^0.4.5",
    "sh3-core": "^0.15.0",
    "svelte": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
