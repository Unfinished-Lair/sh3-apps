{
  "name": "sh3-llm-providers",
  "version": "0.4.0",
  "description": "Unified LLM provider shard for SH3 — registers DeepSeek + Gemini + Claude as sh3-ai.providers, plus per-provider settings views",
  "author": "@unfinished-lair",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "vite build",
    "build:artifact": "vite build --config vite.artifact.ts",
    "dev": "vite build --watch",
    "test": "vitest run"
  },
  "peerDependencies": {
    "sh3-ai": "^0.6.0 || ^0.7.0 || ^0.8.0 || ^0.9.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "sh3-ai": "^0.6.0 || ^0.7.0 || ^0.8.0 || ^0.9.0",
    "sh3-core": "^0.23.2",
    "svelte": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
