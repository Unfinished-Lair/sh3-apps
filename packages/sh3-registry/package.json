{
  "name": "sh3-registry",
  "version": "0.1.2",
  "type": "module",
  "private": true,
  "description": "SH3 registry manager — server shard for hosting and managing a package registry",
  "scripts": {
    "build:artifact": "tsc -p tsconfig.server.json && vite build",
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "vite build"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@types/node": "^25.5.2",
    "sh3-core": "*",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
