{
  "name": "sh3-sync",
  "version": "0.1.0",
  "author": "@unfinished-lair",
  "type": "module",
  "private": true,
  "description": "Hub-and-spoke document sync server shard for SH3",
  "scripts": {
    "build": "vite build",
    "build:artifact": "vite build && node scripts/build-artifact.mjs",
    "test": "vitest run",
    "dev": "vite build --watch"
  },
  "peerDependencies": {
    "sh3-core": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "hono": "^4.6.0",
    "sh3-core": "^0.9.0",
    "sh3-server": "^0.9.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
